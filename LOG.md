# 工作日誌

> 這份是給我自己看的。**卡住的地方寫得比做成的地方詳細**——三個月後回頭看,
> 有價值的是「我以為會怎樣、結果怎樣、為什麼」,不是「我裝了什麼」。

---

## 2026-08-07 — W01：偵察與解包

**目標**：過 G0(工具鏈全綠)+ G1(能口述韌體七要素)

**結果**:G0、G1 都過了,而且做出了計畫外的東西。

---

### 做了什麼

- WSL2 Ubuntu 24.04 工具鏈:binwalk v3.1.0(cargo)、unblob 26.6.4(pipx)、
  sasquatch v4.5.1-6(ONEKEY 預編 deb)、qemu-user-static、flashrom、picocom
- Windows 端:portable Temurin JDK 21 + Ghidra 12.1.2,全部 SHA-256 釘死
- 抓到**兩個**韌體版本並驗證雜湊
- 解出兩個 rootfs,答完 G1 七要素
- 寫了 `fwrecon`(Python,零執行期相依),58 個測試
- 自己逆出 Realtek `IMG_HEADER_T` 容器格式並寫成 parser
- Docker + GitHub Actions CI

---

### 卡住的地方(這段最值錢)

#### 1. `curl | sh` 把錯誤吞掉了

rustup 安裝失敗,退出碼 7。以為是網路掛了,結果 5 分鐘後手動測 `sh.rustup.rs`
回 200——是瞬斷。

真正的問題是我看不出來:`curl ... | sh` 在 `pipefail` 下,退出碼到底是 curl 的
還是 sh 的,分不清,而且沒留下任何可以檢查的檔案。

**改法**:先下載到 cache、再執行。網路錯誤和安裝錯誤從此分得開。
**教訓**:安裝腳本裡的 `curl | sh` 是反模式,不是因為不安全,是因為不可除錯。

#### 2. binwalk v3 編不過 —— fontconfig

`yeslogic-fontconfig-sys` build.rs panic。binwalk v3 用 plotters 畫熵圖,
plotters 要 fontconfig/freetype。裝 `libfontconfig1-dev libfreetype-dev` 就好。

**教訓**:Rust 工具的系統相依不會出現在 `cargo install` 的文件裡,只會在 build.rs
炸掉的時候出現。

#### 3. 我的 G0 驗證誤報工具壞掉

驗證腳本說 `sasquatch` 和 `unsquashfs` MISSING。實際上兩個都好好的。

原因:`unsquashfs -version` **印出正確版本然後 exit 1**。squashfs-tools 的老毛病。
我用「執行成功」當存在判準,就把好工具判死了。

改用 `-help`(rc=0)。

**教訓**:驗證一個工具「能動」,要先知道它「怎樣算能動」。不能假設 `--version` 回 0。

#### 4. 我的探測腳本自己有 bug(這個最丟臉)

為了查上面那件事,我寫了:

```bash
out=$("$t" "$flag" 2>&1 | head -3)
echo "rc=$?"
```

回報全部 `rc=0`,跟我看到的現象矛盾。因為 `$?` 抓的是 **pipeline 最後一個指令**
(`head`)的退出碼,`head` 永遠回 0。

**教訓**:除錯工具本身也會說謊。當「觀測結果」跟「現象」矛盾時,先懷疑觀測方法。

#### 5. launcher 把退出碼吃掉

我的包裝腳本最後一行是 `echo "exit code: $?"`,所以腳本自己的退出碼是 `echo` 的
= 0。背景任務回報「成功」,實際上裡面炸了。連錯兩次才發現。

**教訓**:任何包裝腳本都要 `exit "$rc"`。

#### 6. **`readelf` 在無聲說謊** ← 今天最重要的一件事

我用 `readelf --dyn-syms` 掃兩個版本的 boa,找危險函式 import。

- V2.1.2:181 個 UND 符號,含 `system`、`popen`、`strcpy`……合理
- V3.4.0:**0 個**

我差點就寫下「2020 版移除了所有危險 import」。那會是完全錯誤的結論。

真相:V3.4.0 的 boa 被 **`sstrip`** 過,`e_shnum == 0`,section header 整個沒了。
`readelf` 靠 section header 回答問題,找不到就**印空白、回傳 0**。而 `nm -D` 走
`PT_DYNAMIC`,照樣列得出 `U system`。

```
$ readelf --dyn-syms bin/boa    # 什麼都沒有,exit 0
$ nm -D bin/boa | grep system   # U system
```

**教訓**:「工具沒回報問題」不等於「沒有問題」。無聲失敗比大聲失敗危險得多。
**行動**:這件事直接催生了 `fwrecon/elf.py`——自己解析 ELF,只走 program header
和 `PT_DYNAMIC`(即 loader 走的路),這兩個東西 strip 不掉。

#### 7. 然後我自己的 parser 也錯了

`fwrecon elf` 跑 V2.1.2 的 boa:16 imports / **257 exports**。但 `readelf` 說有
181 個 UND。

我把 import 判準寫成 `st_shndx == 0 && st_value == 0`。**MIPS 上這是錯的**:
未定義函式的 `st_value` 存的是 `.MIPS.stubs` 裡 lazy-binding 樁的位址,通常非零。
所以 181 個裡有 165 個被誤判成 export——包含 `system` 和 `strcpy`,正好是這支工具
存在的理由。

ABI 的定義只有一條:`st_shndx == SHN_UNDEF` 就是 import。`st_value` 不該參與判斷。

修完:V2.1.2 的 command-exec binary 從 0 個變成正確的 33 個。

**教訓**:
- 寫了新工具,一定要跟既有工具對答案。我是因為 `nm -D` 說 181、我說 16,才發現的。
- 測試 fixture 要重現「陷阱」,不是重現「教科書案例」。我把 fixture 改成 import 帶
  非零 stub 位址(跟真實 MIPS 一樣),既有測試就自動變成這個 bug 的回歸測試。

#### 8. 工具的假陽性 / 假陰性

跑真實韌體後才看得出來的兩個問題:

- **gzip 假陽性**:`\x1f\x8b` 只有 16 bit,在壓縮資料裡到處都是。在 LZMA kernel
  裡報了一個不存在的 gzip。加上 deflate method byte(`\x08`)就乾淨了。
- **`/init` 被當成 shell script 解析**:它其實是 busybox ELF,結果報告裡出現一整行
  反編譯亂碼。改成看內容(有 NUL byte 就跳過)而不是看檔名。

**教訓**:單元測試過了不代表工具對。要拿真實資料跑,而且要**讀輸出**,不能只看有沒有
crash。

#### 9. winget 裝 JDK 卡在 UAC

`winget install EclipseAdoptium.Temurin.21.JDK` 走 MSI,要管理員權限,跳 UAC 就卡死。
改成下載 Temurin **portable ZIP** 解到 `%LOCALAPPDATA%`,不用權限,而且能釘 SHA-256。

**教訓**:要能無人值守執行的東西,別走需要提權的安裝路徑。

#### 10. PowerShell 5.1 的 `2>&1` 陷阱

`& $javaExe -version 2>&1` 讓驗證腳本掛掉——JDK 和 Ghidra 明明都裝好了。
Windows PowerShell 5.1 對原生程式做 stderr 重導向,會把每一行包成 ErrorRecord,
配上 `$ErrorActionPreference='Stop'` 就變成終止錯誤。

`java -version` 寫 stderr(歷史包袱),JDK 9+ 的 `java --version` 寫 stdout。改用後者。

---

### 學到什麼(技術面)

- Realtek `.web` 是 `cvimg` 容器:每段 16-byte big-endian header
  `{signature[4], startAddr, burnAddr, len}`。`burnAddr` 直接給出 flash 佈局——
  這是之後拿實體 dump 對照的依據。
- **SquashFS 4.0 的 on-disk 格式規定就是 little-endian**,不管 CPU 端序。所以
  big-endian MIPS 韌體裡看到 `hsqs` 是正常的,不是矛盾。v3 才是端序相依的。
- 兩份韌體的 SquashFS `mkfs_time` 都是 2038 年——把 raw 值 byte-reverse 之後,
  數值幾乎等於檔案系統大小。八成是廠商 build script 把 size 寫進 timestamp 欄位。
  結論:**這些映像不能用自己的 metadata 定年**。

### 學到什麼(方法面)

今天六個 bug 裡,有四個是**觀測工具本身出錯**,不是被觀測的東西出錯。
在逆向工程裡這個比例大概是常態,因為每件事都是間接觀測。

實務原則:**任何結論都要有兩個獨立來源**。今天救我的分別是
`nm -D` vs `readelf`、`nm -D` vs `fwrecon`。

---

### 意外收穫

原本只打算「解包 + 記錄」,結果拿到了 2015 和 2020 兩個版本,跨越 2019-12 揭露事件,
可以做真正的前後對照:

- **2015 版(揭露後 5 週)**:`/bin/skt` 後門還在,只是 `rcS` 裡那行被註解掉(`#skt&`)。
  「拿掉後門」和「不再啟動後門」是兩件事,他們只做了第二件。
- **2020 版(全面揭露後 9 個月)**:`/web/config.dat` 是指向 `/var/config.dat` 的
  symlink,而 `rcS` 會 `cp -rf /web/* /var/web/`。也就是 CVE-2019-19822 的暴露路徑
  在結構上還在。

  ⚠️ **這還不能說「沒修」**。修法也可能在 Boa 的請求授權邏輯裡,而不是檔案佈局。
  要在 W03 進 Ghidra 確認。先記錄證據,不要超譯。

---

### 收工前多做的十分鐘(結果比預期好)

寫 RUNBOOK 的時候我寫了一道 `qemu-mips-static` + chroot 的指令,按照自己定的規則
「只寫跑過的東西」,就去實測了一下。結果:

```
$ sudo chroot $ROOTFS /qemu-mips-static /bin/busybox
BusyBox v1.13.4 (2015-08-11 17:26:34 CST) multi-call binary

$ sudo chroot $ROOTFS /qemu-mips-static /bin/boa --help
Usage: /bin/boa [-c serverroot] [-d] [-f configfile] [-r chroot] [-l debug_level]
```

**2015 年的大端序 MIPS 執行檔,在我的 x86 筆電上跑起來了。** 而且 `boa` 吐的是它
自己真正的用法說明,不是崩潰訊息。

這把計畫書裡列為 W05 風險的「模擬環境」先解掉一半 —— 大概率**不需要 FirmAE**
(全系統模擬,要裝 30–60 分鐘)。

⚠️ 但別高興太早,講清楚這證明了什麼:**只證明「載得起來、跑得動」**。真的要服務
HTTP 請求時,`boa` 會呼叫 `libapmib.so`,而 apmib 會直接讀 `/dev/mtd*` 快閃記憶體
分割區 —— chroot 裡沒有那些東西。那是 W05 要處理的。

**方法上的收穫**:我原本只是要驗證文件裡的一行指令,結果順手把下下週的一個風險
提前解掉了。**「只寫你跑過的東西」這條規則的價值,不只是文件正確,而是它逼你真的
去跑。** 這次是撿到,但邏輯上不是偶然。

### 明天 / 下一步

1. Ghidra:找 Boa 的 handler dispatch table,把 `formSysCmd` 的真實註冊名挖出來
   (字串表裡沒有 `formSysCmd`,但 `sysCmdselect`、`/tmp/syscmd.log` 都在)
2. 確認 Boa 對 `.dat` 路徑到底有沒有做認證檢查
3. 逆 `libapmib.so` 的 `COMPCS` 格式
4. 硬體到貨後:量 UART、讀 flash 型號(驗證 2MB vs 3.57MB 的矛盾)、dump 出來跟
   `burnAddr` 佈局對照

### 待辦(不緊急但別忘)

- [ ] archive.org 那份 V2.1.2,`r6cr` 宣告長度比檔案多 9 bytes。是上傳者截斷,還是
      長度欄位含某種 trailer?找第二份來源比對
- [ ] Softpedia 的 2.1.1 / 2.1.3 / 2.1.6 有 interstitial 擋腳本下載,手動抓
- [ ] `w6cg` 那個 web bundle 的封裝格式只看了個大概,還沒寫 parser
- [ ] kernel(LZMA @ `cr6c+0x2808`)還沒解

---

## 2026-08-10 — W03:Ghidra 靜態逆向上半

**背景**:W02 的硬體還沒到。計畫書寫「G2 過不了不准進 W3」,但 G2 的通過標準
本來就允許「官方韌體主路徑 + 誠實記錄」,所以硬體不是知識上的前置條件,只是
時間上的。先做 W03。

**結果**:本週 DoD 五項全過,而且 W04 的 G3 八格裡有七格順便解掉了。

---

### 做了什麼

- 把 `root_form[]` 分派表從兩個 build 裡挖出來(2015 版 59 個 handler、
  2020 版 49 個),並且**用「誰讀這張表」當證據**分辨它跟 ASP 變數表
- 把 handler 全部命名寫回 Ghidra 專案(185 個),不是手動改 5 個
- 讀完 `process_header_end`、`translate_uri`、`handleForm`、`formLogin`、`formWsc`
- 逆完 `/bin/skt`(10 KB,36 個函式,可以整支看懂)
- 新增 4 支 Ghidra 腳本:`BoaFormTable`、`BoaSinks`、`BoaDecompile`、`BoaListing`

---

### 今天最重要的一件事:**授權檢查是拿 URI 做子字串比對**

`process_header_end` @ `0x0040be0c`,整個授權區塊的進入條件是
`strstr(uri, "htm") != NULL`:

```
0040c23c  jalr t9                  -> strstr
0040c248  beq v0,zero,0x0040c3a0                 ; 回 NULL 就跳過整段檢查
```

所以路徑裡沒有 `htm` 三個字的東西,**完全不檢查**:`/config.dat`、`/ca.cer`、
還有全部 59 個 `/boafrm/form*`。

CVE-2019-19822 的公告寫的是現象——「`.dat` 檔沒有被限制」。這裡是原因,而且
範圍比公告大得多:`.dat` 根本不是特例,它只是「不是 .htm」。

⚠️ **這是靜態結論**。機器還沒開過。要證實只需要三個 `curl`,寫在
`notes/auth-flow.md` 最後。在那之前,只能說「程式碼是這樣寫的」,不能說
「機器是這樣跑的」。

---

### 卡住的地方(這段最值錢)

#### 1. W01 的 `import.ps1` 把自己的輸出蓋掉了

想重用 W01 建好的 Ghidra 專案,結果裡面**只有一個 program**,叫 `boa`。

原因:`analyzeHeadless -import <path>` 是用**檔名**命名 program 的。兩個版本的
`boa` 都叫 `boa`,加上 `-overwrite`,第二次匯入就把第一次的無聲蓋掉。
`import.ps1` 裡明明算了 `$programName = "boa-$Label"`,但那個變數**從頭到尾沒被用到**。

W01 產出的 JSON 其實是對的(各自在自己那次匯入時寫出),但兩個檔案的 `program`
欄位都是 `"boa"`,**除了檔名以外沒有任何東西能證明哪個是哪個**。

改法:每個版本一個 project folder;而且每份 Ghidra 報告都帶上被分析檔案的
SHA-256,`check-reports.py` 現在強制要求。

**教訓**:一份無法指認自己輸入的報告,不是證據。而且「變數算了沒用到」這種 bug,
編譯器不會抓,腳本語言更不會。

#### 2. sink 統計第一版是假陰性 ← 跟 W01 的 `readelf` 是同一種錯

第一次跑出來:V2.1.2 有 **589** 個 `strcpy` 呼叫點,V3.4.0 有 **1** 個。

我差點就寫「2020 版把不安全字串複製都拿掉了」。但 `nm -D` 明明說 V3.4.0 還在
import `strcpy`,而且兩個 build 是同一份程式碼、大小只差 11%,`sprintf` 兩邊都是
694 個。**一邊在說謊。**

真相在 ELF 裡:

| | V2.1.2 | V3.4.0 |
|---|---|---|
| section header | 29 個 | **沒有**(`sstrip` 過) |
| `DT_MIPS_PLTGOT` | 沒有 | `0x472ac4`,有真的 PLT |
| 呼叫方怎麼到 `strcpy` | Ghidra 建了叫 `strcpy` 的 thunk | `jal` 到一個**沒有名字**的 PLT stub |

沒有 section header,Ghidra 找不到 `.plt` 去標它的每一項,結果只有部分 PLT 項目
被建成函式——`system`、`sprintf` 有,`strcpy`、`strcat` 沒有。

修法不用猜。MIPS 的 PLT entry 是四道指令,每個欄位都由 GOT slot 位址 `S` 決定:

```
lui   $15, %hi(S)        3C 0F hi
lw    $25, %lo(S)($15)   8D F9 lo
addiu $24, $15, %lo(S)   25 F8 lo
jr    $25                03 20 00 08
```

所以我是**把 signature 算出來**再去記憶體找,而且規定「只能命中一次,命中兩次或
零次就不採用」。修完:587 vs 577,兩個 build 對得上了。

修之前先量過:`jal = 9979`、`jalr = 16`。幾乎全部都是直接跳 PLT,所以根本不需要
去解 `gp`——**先量再修,不要先猜**。

順手還修了兩件事:
- 從**函式外面**來的 data reference 是 GOT 欄位,不是呼叫點。就是它讓 `strcpy`
  回報「1」而不是誠實的「0,而且 0 是錯的」。
- 報告加了 `self_check`:如果某個 symbol 明明被 import 卻找不到呼叫方,整份檔案
  標成 `SUSPECT`。

**教訓**:工具回報「0」也是一種主張,要跟其他主張一樣被質疑。這已經是這個專案
第二次栽在同一件事上了(W01 是 `readelf` 對 sstrip 過的 ELF 印空白)。

#### 3. 公開的 SDK 原始碼跟手上的 binary 不一樣

網路上流傳的 rtl819x SDK 把分派表元素宣告成:

```c
typedef struct { char name[80]; void (*function)(request*, int, char**); } form_name_t;
```

名字是**內嵌**的,一項 84 bytes。我本來打算照這個寫 recovery 腳本。

實際上這兩支 binary 是 `char *name`,一項 8 bytes。證據有兩個:`handleForm` 自己
的 `ppuVar5 + 2`(在 `char**` 上就是 8 bytes),還有復原出來的表格項數
(59 / 49)剛好等於 W01 用數字串數出來的數量。

**教訓**:外流的 SDK 是「對眼前 binary 的假設」,不是「規格」。所以腳本寫成
「測試 `[字串指標][可執行位址]` 這個形狀是否以固定間距重複」,而且**要能失敗**——
一支不可能失敗的復原腳本,證明不了任何事。

#### 4. 反編譯器在最關鍵的函式上出警告,而我差點沒看

`process_header_end` 的反編譯輸出頂上掛了三行
`WARNING: Heritage AFTER dead removal` / `Restarted to delay deadcode elimination`。
而它反編譯出來的內容是:拿使用者送的帳密去跟**兩個從來沒被寫入過**的堆疊
buffer(`sp+0x40`、`sp+0x60`)比較,比中了就給 `authorized = 2`(比一般帳號還高一級)。

「binary 真的拿未初始化的堆疊當密碼比」和「反編譯器把填值的呼叫吃掉了」是完全
不同的兩件事。所以我寫了 `BoaListing.java` 去讀組語,結果:整支函式裡對 `sp+0x40`
和 `sp+0x60` 只有三次存取,兩次是拿位址當 `strcmp` 參數、一次是讀第一個 byte。
**沒有任何寫入,位址也從來沒被傳出去過。**

所以它真的在跟未初始化的堆疊比。但**能不能利用是動態問題**,靜態決定不了——
記錄成 W05/W06 的候選,不是結論,而且在證實或否證之前不會報給任何人。

**教訓**:反編譯器自己宣告它有困難的時候,任何從它輸出得到的結論都是猜的。

#### 5. 我的 `string:` selector 無聲地找不到東西

想在 2020 版裡找授權函式,用 `string:AUTHG_IP_ADDR`、`string:getSanvas` 去撈,
回傳 0 個函式。差點就寫「2020 版沒有這些東西」。

其中一半是真的(2020 版確實把 `AUTHG_IP_ADDR`、`countDownPageWizard.htm`、
`notice_frame.htm`、`formLogin.htm` 全刪了,授權那段是重寫的),
但 `getSanvas` 是我的 bug:實際字串是 `setting/getSanvas`,**reference 指向字串
開頭**,我搜子字串命中的是中間那個位址,那裡當然沒有 xref。

**教訓**:自己寫的查詢工具回傳空集合時,先問「是真的沒有,還是我問錯了」。
這跟第 2 點是同一個病。

#### 6. W01 的「最高價值函式」是假陽性

W01 把 `FUN_00440eec`(裡面有 `cp /var/web/config.dat %s`)寫成「W01 找到的
單一最高價值函式」。W03 追下去:它是 `/boafrm/formSaveConfig`,而那個 `%s` 是

```c
sprintf(buf, "/var/web/Config_%s_%04d%02d%02d%02d%02d%02d.bin", "N150RT",
        年, 月, 日, 時, 分, 秒);
```

一個寫死的型號字串加六個 `localtime()` 欄位。**沒有任何請求資料進得去,不是
命令注入。**

有寫進 `sink-inventory.md`。死掉的候選跟活下來的候選一樣要留下來,不然這個專案
就會變成「只記錄成功」的專案。

---

### 學到什麼(技術面)

- **`formSysCmd` 在這台機器上不存在**。不在 2015 的 59 項裡,也不在 2020 的 49 項裡。
  而 `handleForm` 是 `strlen` 相等 + `memcmp` 全等比對,沒有 fallback、沒有第二張表。
  W01 提名的 `FUN_0044c610` 是 **ASP 變數表**裡的 `sysCmdLog`,由 `handleScript` 讀——
  是「顯示 log 的那半」,會執行命令的那半根本沒編進來。
- **真正的命令執行面是 `formWsc`**。`localPin`、`peerPin` 沒過濾也沒長度檢查就進
  `system()`;`targetAPSsid` 有長度檢查(< 33)但被塞進 shell 的雙引號裡沒跳脫。
  同一支函式裡十行前的 `targetAPMac` 卻是逐字元過濾成 hex 又要求剛好 12 長。
  **會寫過濾,但每個參數各自為政**——這就是 ad-hoc 輸入處理的長相。
  而且 2015 和 2020 兩版**一模一樣**,五年沒動。
- **`/bin/skt` 全解了**:聽 TCP **5555**,`hel,xasf` 執行
  `iptables -I INPUT -p tcp --dport 80 -i eth1 -j ACCEPT`,`oki,xasf` 刪掉。
  它不給 shell、也不繞密碼,它是**可達性後門**——把本來防火牆擋住的管理介面打開。
  配上上面那個 `htm` 授權洞,2015 那份映像裡就是一條完整的未認證遠端 root 鏈,
  由兩個各自出貨的缺陷組成。

### 學到什麼(方法面)

W01 的結論是「任何結論都要有兩個獨立來源」。W03 把它變得更具體:

**當兩個來源不一致的時候,不一致本身就是資料。**
- `nm -D`(有 strcpy)vs 我的統計(1 個)→ 指向 sstrip + PLT
- SDK 原始碼(`char[80]`)vs 反編譯(`+2` 步進)→ 指向這台是不同的 SDK 設定
- W01 的字串計數(59)vs 復原出來的表(59)→ 一致,這才是真的可以拿去用的數字

還有一個新的:**復原資料結構 > 累積交叉引用**。`formSysCmd` 那三條線索
(字串不見了、有 log 路徑、有 `sysCmdselect` 頁面片段)全都指向同一個錯誤答案。
把 `root_form[]` 這個結構挖出來,一次就結案了。

### 明天 / 下一步(W04)

1. 2020 版的授權是重寫過的,**還沒讀**。第一步:找出誰呼叫 `FUN_0040b850`
   (吐 `WWW-Authenticate` 的那支)。我的 `callers:` selector 回傳空的,那是工具
   結果不是答案。
2. `libapmib.so` 完全沒讀,而它在本週每一個發現的路徑上。
3. 六個 handler 的 `execl` argv 還沒追。
4. `DAT_0048e9f8` 的大小(401 路徑上那個 `strcpy(全域, URI)`)。
5. 硬體到貨後:用三個 `curl` 驗證這週所有結論。

---

## W04 — 2026-08-11:G3 過了,但過的方式跟預期的不一樣

### 先講結論

G3 五條全過。可是 W03 留下來的兩個開放問題,**答案都跟 W03 猜的相反**:

1. 2020 版**有修**(所有 POST 都進閘門了),但**沿用同一種寫法**,所以又開了一個新洞。
2. 2025 那批 CVE **就是掛在這台機器上的**,不是 W03 寫的「掛在兄弟型號」。

還有一個更難堪的:W01 寫的「兩份映像檔裡都沒有 `/etc/passwd`」是錯的。
兩份都有。而且 2015 那份裡面,**Pierre Kim 2015 公告的後門帳號 `onlime_r` 還在**,
uid 0,雜湊值跟他公告上印的一模一樣。

### 這週最有價值的一件事:三次工具錯誤

新寫的 `BoaArgTrace` 連續錯三次,**三次它自己的 `self_check` 都寫 `consistent`**。

| 第幾次 | 症狀 | 真正的原因 |
|---|---|---|
| 1 | 304 個呼叫點只有 **1** 個被標成「有請求參數流進來」 | 同一套「把 varnode 解成字串」的邏輯寫了兩份,兩份走鐘了 |
| 2 | 2015 版 86 個、2020 版 **0** 個 | `accessor:` 選項拿去跟小寫化的名字比,所以永遠不會相等 |
| 3 | `strcpy` 在 2015 版 151 個、2020 版 **0** 個 | W03 已經在 `BoaSinks` 修過的 sstrip PLT 問題,我重寫了一份沒帶上修正 |

第 1 次抓到,是因為 W03 早就**用手讀出** `formWsc` 有三個參數進 `system()`,
工具卻一個都沒找到 —— 兩個來源不一致。
第 2、3 次抓到,靠的是 CLAUDE.md 那條:**兩版要橫著讀,不要直著讀**。
同一份程式碼相隔五年,不可能 86 → 0。

得到的教訓,寫在這裡因為它比任何一個漏洞都值錢:

> **一個永遠不會觸發的檢查,也永遠不會失敗。**
> `self_check: consistent` 只代表「我檢查的那幾件事沒出問題」,
> 不代表「答案是對的」。

所以做了兩件事:
- 把 PLT 解析抽成 `BoaPlt.java`,**只留一份**。同一個 bug 在這個專案出現兩次,
  就不是 bug 了,是設計問題。
- `BoaArgTrace` 新增 `accessor_declared_but_never_matched`:
  **給了選項卻沒配對到任何東西,現在算錯誤,不算沉默。**

### 一行程式碼 = 兩個 CVE

```c
sprintf(acStack_220, "flash set HW_WLAN0_WSC_PIN %s", localPin);  /* 100 bytes */
system(acStack_220);
```

- 沒過濾 → CVE-2025-3987(命令注入)
- 沒長度檢查 → CVE-2025-4462(緩衝區溢位)

同一行。而且**2015 那份映像檔裡一字不差**,比 CVE 早十年。

`submit-url` 那組更誇張:四個 CVE(3990/3991/3992/3993),其實是**同一段三行的尾巴
複製到 34 個 handler 裡**。有編號的那四個只是抽樣,不是全部。

### `strcpy(p, "/status.htm")` —— 沒人寫進 CVE 的那個

```c
pcVar1 = req_get_cstream_var(param_1, "submit-url", "");
if (*pcVar1 == '\0') {
    strcpy(pcVar1, "/status.htm");   /* 寫進 accessor 回傳的 buffer */
}
strcpy(&lastUrl, pcVar1);            /* lastUrl 只有 100 bytes */
```

`req_get_cstream_var` 找不到參數時,回傳的是**呼叫端給的預設值**,也就是 `""`
那個字面常數 —— 它在 `.rodata`,而 `.rodata` 在 `R E` 的 segment 裡。

所以「POST 過去但完全不帶 `submit-url`」→ 往唯讀記憶體寫 12 bytes → boa 死掉。
2015 版連認證都不用過。

這也順便解釋了一件本來看起來像雜訊的事:**這個型號所有公開 PoC 都帶
`submit-url=/xxx.htm`**。不是為了好看,是不帶就沒有回應。

**但這是靜態讀出來的。** 一個 `curl` 就能證實或推翻,而機器還沒到。

### 2020 版:修了,可是

閘門條件從「URI 含 `htm`」變成「URI 含 `.htm` 或 `.asp`,**或者是 POST**」。
所有 handler 都進去了 —— **這是真的修好了,要講清楚。**

問題是判斷方式沒變,還是拿 `strstr` 掃整條 URI,而豁免名單完全沒有錨定:

```
POST /login/boafrm/formWsc
      ^^^^^ 含 "login"     -> 0x0040a2d8 跳過轉址
            ^^^^^^ 含 "boafrm" -> translate_uri 放行 POST
            ^^^^^^^^^^^^^^^^ handleForm 用 strstr 找 "/boafrm/",再精準比對
```

三個 `strstr`,同一條字串,沒有一個綁開頭。**照程式碼讀,這樣就進 handler 了。**

`GET /config.dat` 更直接:不含 `.htm`、不含 `.asp`、不是 POST → 閘門整段跳過。
CVE-2019-19822 在一份 2020-10-30、也就是全面揭露九個月後的版本裡,還在。

> 這條**沒有回報給任何人**,也不會在 W05/W06 實機或模擬跑出來之前回報。
> 靜態讀三個 `strstr` 不等於漏洞。

### 順手撿到的

- `/etc/privateKey.key`(2020 版):2048-bit RSA 私鑰,自簽憑證 CN=192.168.1.254,
  **2014 年就過期了**,而這份韌體是 2020 年出的。每一台都同一把。
- `/etc/dropbear_rsa_host_key`(2015 版):同樣的問題,SSH host key 全型號共用。
- `root` 的密碼兩版都是 `123456`。Pierre Kim 公告上是 `12345`。
  廠商對「密碼被公開」的回應是加一位數。
- `nobody` 的 uid 和 gid **都是 0**。

### 明天 / 下一步(W05)

1. **W02 還是卡在硬體。** 但 W05 的模擬路線 W01 已經驗過能跑 —— 先把 boa 在
   qemu chroot 底下服起來,`/dev/mtd*` 用假檔案墊。
2. 四個 `curl` 就能結案的東西,列在 `notes/auth-flow-2020.md` 最後。
   其中 `POST /login/boafrm/formWsc` 是整週最重要的一個。
3. 去抓 **V2.1.1-B20150708**(Pierre Kim 說最後一個有洞的版本),
   把它的 `root_form[]` 挖出來 —— 一個指令就能確定 `formSysCmd` 到底是
   「廠商修掉的」還是「本來就沒編進去」。
4. `TELNET_ENABLED` / `SSH_ENABLED` 預設是不是開的?這決定 `root:123456` 值多少。
5. `needReboot`、`run_init_script_flag` 是誰在讀?它們就貼在 `lastUrl` 後面。

---

## W02 Day 1 — 2026-08-14:硬體到貨,而我第一個動作就做錯了

### 先講結論

**W01 贏了一次。** 硬體到貨前三週,W01 從 `.web` 容器自己的 `burnAddr` 表算出
flash map 最高到 3.57 MiB,斷定公開規格寫的 2MB **不可能對**,預測至少 4MB。

今天 `U19` 的絲印是 `cFeon QH32B-104HIP` = EN25QH32B = **32 Mbit = 4 MiB**。

這是這個專案第一次**對物理世界做出可證偽的預測,然後物理世界同意了**。

同一天,計畫書的硬體規格表被打掉三格:SoC 是 **8196E 不是 8196C**、
RAM 晶片是 **32MB 不是 16MB**、flash 是 4MB。三格全部都是從別人的規格抄來的,
沒有一格是量出來的。

**但今天真正值得寫下來的不是這個,是我一開始做錯的事。**

### 今天走錯的路(這段最值錢)

#### 1. 拆機第一個動作是拿 450°C 去燒天線焊點

烙鐵調到 450°C,板上其他焊點一碰就化,天線那個焊點只有一點點形變。
我當時問的問題是:**「是不是材質不同?是不是我不夠有耐心?是不是溫度不夠高?」**

**三個都不是,而且我問錯問題了。**

技術上的答案是**熱容量,不是溫度**:那個焊點另一端是 RF 接地銅箔加過孔陣列,
對烙鐵來說是一片散熱片;顯示的 450 是尖端溫度,焊點大概只有 150–200°C;
而且 450°C 會讓助焊劑在你需要它工作之前就碳化燒光,**溫度越高反而越焊不動**。
有鉛 183°C、無鉛 217–220°C,兩個都遠低於 450 —— **熔點從頭到尾就不是問題**。

但這只是次要的答案。真正該先問的是:

> **我為什麼要拆這條天線?**

我答不出來。G2 要的四樣東西 —— bootlog、SPI dump、dump 對官方韌體、標註過的
PCB 照片 —— **天線在不在板上,一樣都沒有幫助**。

而那條線的終點是 **RTL8188ER 的輸出級**,拆掉通電等於對開路發射;
**而且這台是 G2 和 G4 的單點故障**,W05 要把 web server 服起來、W06 要在實機上
重現 CVE,都靠它,沒有備品。

**寫成規則:任何不可逆的動作,先答出「這一刀換到哪一個 gate 的哪一格」。答不出來就不要動。**

#### 2. 差點把 on/off 開關的線剪掉扭在一起

想法是「剪掉接一起變成常開,插回板子」。**兩個問題:**

第一,**我不知道那兩根線是什麼。** 可能串在 DC 電源路徑上(短接 = 常開,能動),
也可能接到 GPIO(短接的意義取決於極性)。從照片看 `J2` 緊貼著 DC 座,layout
像前者 —— **但那是從照片推的,不是量的**,而量它只要三分鐘。

第二,**就算量出來能短接,還是不該剪。** 接下來抓 bootlog 要反覆斷電重開幾十次,
**一個能用手撥的開關是資產,不是障礙**。我是在把一個乾淨的動作,換成一個會抖、
會斷續、會短到旁邊的動作。

結果:插頭插回去,開關撥 ON。零刀。

#### 3. 還沒拍照就先動烙鐵

**G2 的第四格交付物就是標註過的 PCB 照片,而原廠狀態只有一次機會拍。**
我在拍照之前就先去燒焊點了。這次沒造成損失,是因為焊點沒化開。

### 五顆 IC

| 位置 | 絲印 | 是什麼 |
|---|---|---|
| — | `RTL8196E` `I510VG1` `GF23 TAIWAN` | SoC |
| `U19` | `cFeon QH32B-104HIP` `1750HKB` | EN25QH32B,**4 MiB** SPI NOR |
| — | `Winbond W9825G6KH-6` `1837H` | 256 Mbit SDRAM,**32 MiB** |
| — | `RTL8188ER` `I210QP1 GF08` | 1T1R 802.11n |
| — | `LSC LSP5526` `181525` | **沒查到**。從位置推測是降壓穩壓 |

外加板子層級:4 LAN + 1 WAN、三顆 U&T 磁性元件(2+2+1 = 五個埠,對得上 8196E
內建的交換器)、`ANT1` / `ANT2` 兩個天線腳位但只裝一支、`U6` 附近有沒裝的焊盤。

**最好的消息:板子下緣有一組已經焊好的 4-pin 排針,旁邊絲印直接印著 `UART`。**
整個 W02 一刀都不用焊 —— 本週最大的不可逆風險在還沒開始之前就消失了。

> 但絲印寫 `UART` 只命名了這一組,**沒告訴我四支的順序**。GND/VCC/TX/RX 明天用
> 電表量,不照慣例猜 —— 接錯的代價不對稱,TX/RX 反了只是沒訊號,VCC 接錯可能燒 SoC。

### 今天最容易騙到自己的一件事

`flashrom -L | grep -i en25qh` 回報 `EN25QH32 ... 4096 SPI`。

**這不是「flash 是 4MB」的第二來源,而我差一點就把它當成第二來源。**

`flashrom` 的資料庫是**用料號當索引**的,而料號是我從同一塊晶片上的同一行油墨
讀出來、再打進 `grep` 的。它證明的是「**如果**這顆是 EN25QH32,那它是 4096 KiB」,
不是「這顆是 4 MiB」。**兩個來源共用同一個上游,就只是一個來源。**

**跟 W01 那個坑是同一種:** `readelf` 和 `nm -D` 在 sstrip 過的 ELF 上不是獨立
來源,因為它們讀同一張 section header。

真正獨立的是**晶片自己在 SPI 上回報的 JEDEC ID**。Day 4 才有。
所以今天筆記裡那張表的「第二來源」欄位**五格全是空的**,那是它應該有的樣子。

### 一個工具的小裂縫

`flashrom --version` 印 `flashrom unknown`,而 `dpkg` 說 `1.3.0-2.1ubuntu2`。

功能完全正常(Debian 打包沒把版本字串編進去),但值得記:**G0 的宣稱是
「每個工具都用跑跑看來驗證,不是檢查檔案在不在」—— 而 `flashrom` 是那張表裡
唯一一個版本號不是跑出來的。** 無害,但表格應該說清楚那個數字哪來的,
而不是讓人以為做了一個其實沒做的檢查。

### 照片的處理

PCB 背面條碼是 12 個十六進位字元,**幾乎確定是這台的 MAC**;正面 QR 標籤是序號。
**兩張都要遮掉才能 commit**,而 G2 要交的就是背面那張。

同一條規則接下來還會用兩次:bootlog 會印 MAC(照 W04 的 `HW_WLAN0_WSC_PIN`
來看,很可能連 WPS PIN 一起印),flash dump 的 config 分割區裡全部都有。

**一條規則:從「我這一台」讀出來的東西一律遮,只發表對「這個型號」成立的事實。**
而且要在 `git add` 之前決定 —— 推上去之後才遮的不叫遮。

順帶一個容易漏掉的:**QR 比印出來的數字危險。** 印出來的號碼要有人去讀,QR 是
自動被解碼的,而且縮圖之後照樣解得出來。所以連廣角照裡只有幾十像素寬的那個
也蓋掉了。

### 兩支新工具,而且兩支第一次跑都是錯的

照片的遮蔽和標註全部寫成腳本,不用影像編輯器。理由跟 W03 拒絕用 Ghidra 截圖
一模一樣:**編輯器產出的東西沒人能檢查、沒辦法 diff、原圖重拍就重現不了。**

- `tools/redact-photo.py` —— 實心塗黑(不是模糊,模糊在已知字體上是可逆的)、
  丟掉 EXIF(手機照片帶 GPS 和裝置 id,那是**視覺遮蔽蓋不到的第二個識別碼**)、
  寫完之後把檔案讀回來自我驗證。
- `tools/annotate-photo.py` —— 框和文字寫在 `pcb-top-annotations.json` 裡,
  圖是算出來的。框移動了,`git diff` 看得到數字變。

**然後它們錯了三次,一次都不是自己抓到的。**

#### 1. 守衛測試報 5/5 全過,而它們全過是因為每一次呼叫都在 `import PIL` 就死了

五個「這個一定要失敗」的測試全部通過 —— **理由全錯**。

原因:login shell 的 PATH 上有 `~/.venvs/thermal/bin/python3`(**另一個專案的
venv**),它剛好裝了 Pillow;而 `bash script.sh` 不是 login shell,`python3` 解析
到 `/usr/bin/python3`,那支沒有 Pillow。於是每個呼叫都非零離開,而我的檢查只看
離開碼。

**抓到它的是「對照組」那一行:「一個合法的呼叫必須成功」。** 我會寫那一行,只因為
CLAUDE.md 那條「recovery script 要能失敗,不能失敗的證明不了任何事」。

修了兩層:直指專案自己的 venv(不是裸的 `python3`),而且每個守衛改成
**必須因為自己那條錯誤訊息失敗**,不是「隨便什麼理由失敗都算」。

> 這跟 W04 的「`self_check` 連寫三次 `consistent`」是同一種病。
> **一個永遠不會觸發的檢查,也永遠不會失敗;一個因為錯誤理由觸發的檢查,更糟 ——
> 它會給你「已經驗過了」的錯覺。**

#### 2. 對照組接著也失敗了,而錯的是檢查器,不是遮蔽

後置條件寫的是「框裡每個像素都必須剛好等於 0」。**那在 JPEG 上永遠不可能成立** ——
純黑方塊重新編碼時會對著自己的邊緣產生振鈴。

而那個振鈴**不帶任何原始資訊**:底下的 block 在編碼器看到之前就已經在 RGB 空間
被整片換掉了。所以檢查該拆成兩半:

- 整個框的最大通道值有一個寬鬆上限 → 抓「框根本落錯地方」
- 框往內縮兩個 MCU 之後必須**剛好全黑** → 這才是真正保證像素被換掉的那一半

順便把存檔改成 `subsampling=0`,讓 MCU 維持 8×8,內縮的計算才站得住。

#### 3. 標註工具默默產出了一張看起來完成、實際是垃圾的圖

兩欄圖例互相蓋住、文字衝出畫面外,而它印 `ok`。

現在它會自己挑欄數和字級,**排不下就報錯**。

> **一張互相重疊的圖例還是「看起來像完成品」,那是工具能交給你的最糟的東西。**

#### 還有一件工具做不到的事

`redact-photo.py` 能證明框裡是純黑。**它證明不了框在對的位置。**
那一關是人工的,三張都親眼看過才刪掉未遮蔽的原檔。

### 明天 / 下一步(W02 Day 2)

1. **拍照補齊 + 遮蔽 + 標註**,把 G2 第四格結掉。
2. **電表定腳位**:GND(通斷對屏蔽罩)→ VCC(通電後恆 3.3V,**同時確認是 3.3V
   不是 1.8V**)→ TX(開機瞬間會抖的那根)。
3. **邏輯分析儀量 baud**,不要用試的:`baud = 1 / 最窄脈衝寬度`。
   26.04 µs = 38400、8.68 µs = 115200。UART decoder 解出可讀英文字才算確認。
4. **順手把五顆 IC 的第二來源補掉**:`/proc/cpuinfo`、核心記憶體行、
   `LSP5526` 的輸出腳電壓。都在 bootlog 起來的那十分鐘之內。
5. **量 `J2`**,把「串在電源上還是 GPIO」這題結掉。
6. 有空的話寫 `BoaIsa.java`:數 `/bin/boa` 裡的 `LWL`/`LWR`/`SWL`/`SWR`。
   0 次的話,就證明 Realtek SDK 的 toolchain 到 2020 年都還鎖在 Lexra 的子集上 ——
   那才是 W01 的 ELF header 寫 MIPS-I 的真正原因。
   **注意 qemu 跑得起來不能當證據**,子集程式在完整模擬器上兩種情況都會成功。

---

## W02 Day 2–3 — 2026-08-15:機器開口了,而它跑的是第三個 build

### 先講結論

**這台跑的韌體是 2018-01-10 建的,不是 V2.1.2 也不是 V3.4.0。**

而且那包含 `/bin/boa` —— **這個專案從 W03 開始逆的就是它**。W03 的 `strstr(uri,"htm")`
授權洞、W04 的 `lastUrl[100]` 和 34 個 handler 的 `submit-url` 慣用法,**全部是關於
那兩份映像的敘述,而這台機器一份都沒跑過**。

那些結論沒有錯,repo 一直很小心地寫版本號。但它們**不涵蓋這台**,而 W05/W06 在
這台上做的任何動態驗證,測的是第三個二進位。

另外三件:UART 腳位和 baud 量出來了、bootloader console 搶到了、**W01 從容器推導出
的三個燒錄位址在實機上三發全中**。

### 今天走錯的路(這段最值錢)

#### 1. 用 200mV 檔去量 3.3V —— 而它沒有報錯,它給了我一個看起來像真的數字

電表停在 `200m`(滿刻度 0.2V)去量 3.3V,**差 16 倍**。

要命的不是差 16 倍,是**它回給我的不是「超量程」,而是一個會漂的 `0.x`** ——
那個形狀跟一個真實的、有雜訊的低電壓讀數一模一樣。我當下的反應是「板子是不是沒電」
「表是不是壞了」「還是我的問題」,而正確答案是「檔位」。

**真正的修法不是換探針手法,是先去量一顆電池。**

1.5V 的 AA 讀到 1.6V → 表和檔位都是好的 → 問題一定在板子那側。
**用已知量驗證儀器,再拿它去量未知量。** 這跟昨天 `flashrom` 那件事、跟 W04 的
`self_check` 是同一條:**在你確認工具沒說謊之前,它報的一切都不是資料。**

#### 2. 孤零零一個 `1` 不是「1 歐姆」

量 UART 腳位對地電阻時,pin2/pin3 讀到 `1`。差一點就當成 1Ω 寫下去。

**手動檔電表上,左邊一個 `1`、後面沒有小數點,是「超出量程」。** 換到 20k 檔之後
真實值是 18kΩ 和 15kΩ —— 那才是訊號腳該有的上拉電阻值。

而 1Ω 在物理上根本說不通:訊號腳對地 1Ω 等於短路,那塊板不會動。
**「這個讀數在物理上合不合理」是免費的第二道檢查,而我當時沒做。**

#### 3. PulseView 的 fx2lafw 死路,花了太久

`Failed to open device / generic/unspecified error`。查下去:WinUSB 驅動綁好了、
`fx2lafw-saleae-logic.fw` 韌體檔在、沒有程式佔用它。**三個最可能的原因全部排除,
而它還是打不開。**

真正的原因大概是 FX2 上傳韌體後會用新的 VID/PID 重新列舉,而那個新 ID 沒有驅動 ——
但我沒證實,因為改用 Saleae Logic 2 之後直接就通了(clone 的 VID/PID 就是
`0925:3881`,原廠軟體認得)。

**記在這裡是因為它是個未結案的診斷,不是因為它解決了。**
下次要證實,方法是:讓 PulseView 跳錯之後不要拔線、不要關程式,那時去列舉 USB。

#### 4. 把 CH341A 燒錄器當成 CP2102 插上去

裝置管理員顯示 `USB UART-LPT` 加驚嘆號。看起來像「序列埠驅動沒裝」,
實際上 `VID_1A86&PID_5512` 是 **CH341A 的並列/SPI 模式** —— 那是燒錄器,不是轉接板。

順帶學到一件有用的:**那個驚嘆號對我們完全不重要。**
`usbipd` 轉送的是原始 USB 裝置,Linux 那邊由 `flashrom` 透過 libusb 直接驅動,
**Windows 這側根本不需要驅動。裝了反而可能打架。**

#### 5. `usbipd attach` 失敗,因為 WSL 沒在跑

`There is no WSL 2 distribution running`。我每次呼叫 WSL 都是跑完就結束,
而 `attach` 需要它持續開著。解法是先丟一個 `sleep 7200` 把發行版釘住。

RUNBOOK §12.5 沒寫這條,已補。

#### 6. `FLR` 的 Y/N 確認吃掉了我的下一個指令

```
FLR 80500000 0 100
Flash read from 00000000 to 80500000 with 00000100 bytes ?
(Y)es , (N)o ? --> DB          ← 我的下一行指令被當成答案
Abort!
<RealTek> 80500000 40
Unknown command !
```

**危險的地方不是它失敗,是它失敗得很安靜。** 如果我沒有在腳本裡加對照組,
接下來那個 `DB` 會印出**上一次留在 RAM 裡的舊資料**,而我會把它當成 flash 的內容。

#### 7. `FLR` 用十六進位,`DB` 用十進位

同一套指令、相鄰的兩個命令,兩種進位。`DB <addr> 100` 給你 100 bytes,不是 0x100。

**沒有任何警告,你會拿到一份格式完全正常、長度錯誤的 dump。** 這是那種會安靜地
毀掉後續所有分析的坑。

#### 8. 我猜 config 區在 flash 尾端,猜錯了

`0x3F0000` 讀到全 `FF`,`0x350000` 也是。理由只有「config 通常放尾端」——
**那不是理由,那是慣例。**

實際上 Realtek SDK 把它放在 **`0x010000` 以下**:`0x006000` 是 `H601` 硬體設定、
`0x008000` 是 `COMPDS` 出廠預設、**`0x00C000` 是 `COMPCS` —— 那就是 `config.dat`**。

否定結果照樣寫進 `flash-layout.md`。

#### 9. 計畫書叫我拿探針去碰「機殼 / RJ45 金屬框」找地

**這塊板沒有屏蔽罩,RJ45 是塑膠的。** 這一步在這塊板上不可能執行,
而且它**安靜地失敗** —— 你會量不到蜂鳴,然後以為排針上沒有地。

改用 DC 座外環 + 大電解電容負極,兩者互相驗證。

### 差一點犯、但沒犯的一個

看到 `BusyBox v1.13.4 (2018-01-10)` 的時候,我差一點直接寫「這是第三個 build」。

**沒有直接寫,是因為有一個反證還沒排除:「搞不好 V3.4.0 只是沒重編 BusyBox,
它的時間戳也是舊的」。** 如果成立,那個日期就毫無意義。

去查了兩份官方映像:V3.4.0 的 BusyBox 戳的是 `2020-10-30 09:55` —— **跟它自己的
發行日期同一天**;V2.1.2 的戳在自己發行日前 14 天。**這家廠商發版時整包重編,
所以時間戳確實追蹤建置日期。反證死了,結論才敢下。**

先想「什麼證據會讓我這個結論是錯的」,再去找那個證據 —— 這一步花了三分鐘,
但它是「我覺得」和「我知道」的分界線。

### 意外收穫:照片的遮蔽被回頭證實了

`0x006000` 的 `H601` 區開頭是一串 MAC 位址,而**第一個就是昨天從 PCB 背面條碼
塗掉的那 12 個字元**。

昨天寫的是「幾乎確定是 MAC,但沒查過 OUI」。今天從一個跟印標籤那台機器毫無關係的
來源確認了。

**而遮蔽是在它還只是推論的時候就做掉的。那個順序是對的:先照「東西的形狀」遮,
確認的事後面再說。**

### 三個「兩來源一致」

| | 來源 1 | 來源 2 |
|---|---|---|
| RTL8196E | 封裝絲印 | **bootloader 開頭那段組語拿晶片 ID 暫存器去比對 `0x8196E000`** |
| 32 MiB RAM | W9825G6KH 絲印 | `ramSize: 32M` |
| bootlog 本身 | CP2102 console | 邏輯分析儀解碼同一條線,**兩份逐字元相同** |

第一項特別值得記:**banner 上的「RTL8196E」不是編譯進去的字串,是從矽晶片讀出來
比對的。** 那比絲印硬,也比 Linux 驅動印的 `chip name: 8196C` 硬 —— 而那支驅動
兩行前才說自己在 probe **RTL8186**,它印的是自己程式碼的血緣,不是晶片。

### 明天 / 下一步

1. **完整的 4 MiB dump。** 兩條路:CH341A(快,但有夾具和 5V 風險),
   或 `FLR`+`DB` 走 console(零風險,38400 大約 80 分鐘)。
   **兩條都跑然後比 hash 是最強的做法**,計畫書 Day 6 也是這樣寫的。
2. **JEDEC ID** —— Day 4 唯一沒做到的量測,flash 型號的乾淨第二來源。
3. dump 出來之後把這台的 2018 `boa` 丟進 Ghidra,**三版橫著讀**。
4. `COMPCS`(`0x00C000`)對 `COMPDS`(`0x008000`)做差分 —— W04 卡住的那一格。
5. `LSP5526` 那一顆還是只有一個來源,一次電表就能結案。

---

## W02 Day 4 — 2026-08-16:量了程式器,改了程式器,然後決定不用它

### 先講結論

- **CH341A 是未改的 5V 板。** 這不是查版號查出來的,是量出來的,而且那組數字本身就是結論。
- **試了網路上的 3.3V 魔改,焊完量起來還是 5V。**
- **原因還沒隔離。** 「焊接失敗」是三個候選之一,不是結論 —— 分辨它們只要一次量測,而那次量測今天沒做。
- **決定改走 bootloader `FLR`+`DB`。** 理由不是「比較安全」,是**順序**。

### 量到什麼

夾子夾空氣、USB 通電、黑棒接板上大電解電容負極:

| pin | 訊號 | 量到 | 誰在驅動 |
|---|---|---|---|
| 8 | VCC | 3.3 V | 板上 LDO |
| 3 | WP# | 3.3 V | 被拉到 VCC 軌 |
| 7 | HOLD# | 3.3 V | 被拉到 VCC 軌 |
| **1** | **CS#** | **5 V** | **CH341A 晶片** |
| **6** | **CLK** | **5 V** | **CH341A 晶片** |
| **5** | **DI** | **5 V** | **CH341A 晶片** |
| **2** | **DO** | **5 V** | 上拉軌 |

**判讀不是看有沒有 5 V,是看分佈。** 被板子拉到 VCC 的兩支跟著 3.3 V 走,被晶片驅動的三支全部 5 V —— 所以 CH341A 這顆 IC 自己吃的就是 5 V,它每一支輸出都是 5 V 擺幅。

**pin 8 是 3.3 V 這件事正是陷阱本身。** 它讓整塊板看起來像 3.3 V 的。如果我只量 VCC 就收工,我會夾上去,而且會覺得自己很謹慎。

最難看的是 **pin 2 (DO)** —— 那是 flash 的**輸出**腳,被拉到比它自己的電源還高 1.7 V。「反正只是輸入腳」這個藉口在這條線上不成立。

### 魔改,以及為什麼不寫成「焊接失敗」

照網路上的做法把 CH341A 的 pin 28(晶片自己的 VCC)從 USB 5 V 切開、改接 3.3 V 軌。焊完再量,**還是 5 V**。

直覺是「焊接失敗」。但**這個專案已經有六次是工具在騙人,不是我做錯**,所以先列候選再說:

1. **走線沒有真的斷。** 最常見。腳抬了或線割了,但 5 V 從另一條路(via、內層走線)還是到得了 pin 28。
2. **腳抬起來了,但底部還碰著焊盤。** 間歇性,最難查,而且「有時候會好」比一直壞更危險。
3. **理論本身不完整。** 特別是 `DO` 那一支:它很可能是板上一顆**直接接到 VBUS 的上拉電阻**,跟 CH341A 的供電無關。**如果 CS/CLK/DI 已經掉到 3.3 V、只剩 DO 是 5 V,那不是焊接失敗,那是改成功了但沒改完。**

**分辨這三個的量測只有一次:量 CH341A 的 pin 28 本身。**

- pin 28 = 5 V → 候選 1 或 2,焊接問題。
- pin 28 = 3.3 V 而 CS/CLK/DI 仍 5 V → 我量錯腳了(晶片吃 3.3 V 不可能輸出 5 V)。
- pin 28 = 3.3 V、CS/CLK/DI = 3.3 V、只有 DO 是 5 V → 候選 3,再處理那顆上拉就好。

今天沒量,所以日誌寫的是**「原因未隔離」,不是「焊接失敗」**。
單一假設不算結論 —— 這條規矩在 W03、W04 各救過我一次,沒道理在硬體上就放寬。

> ⚠️ **這塊板現在的狀態是「已改、未驗證」。** 下次拿起來之前必須重量一遍,
> 不可以假設它是 5 V,也不可以假設它是 3.3 V。上電之前先量三個電阻:
> pin 28 ↔ 3.3 V 軌、**pin 28 ↔ VBUS(必須開路)**、3.3 V 軌 ↔ VBUS(必須開路)。
> 中間那一格是防「兩條軌被我橋起來」—— 那會讓座上的 VCC 也變成 5 V,
> **比改之前更糟**,而且外觀上完全看不出來。

### 還沒查的一件事:板上那顆跳線

CH341A 旁邊有一顆三針跳線,目前在 1-2。它可能是座上 VCC 的 3.3V/5V 選擇、可能是晶片供電選擇、也可能是 EPP/UART 模式選擇。**沒量之前不能動它** —— 如果它是座上 VCC 選擇,換過去 pin 8 就變 5 V,那是最糟的方向。

實驗本身零風險(座上空的、夾子夾空氣),但今天優先序排在後面。

### 為什麼決定走 console,而不是把程式器修好

**不可替代的是那 4 MiB,不是那支 3 美金的程式器。**

- `FLR`+`DB` 已經在**這台機器上**跑通過(8/15 讀出 `w6cg`/`cr6c`/`hsqs`/`H601`/`COMPDS`/`COMPCS`),它不是理論路徑。
- 魔改可能再失敗一次,console 不會 —— 它昨天就動了。
- 就算程式器修好,**in-circuit 讀取還有夾子倒灌供電帶不動整塊板的問題**。走 console 一次閃掉兩個問題。

順手把後面幾週的計畫翻過一遍,確認一件事:**W05–W10 沒有任何一個步驟要寫 flash。** W05/W06 全部是網路側的 `curl` 和 telnet。唯一提到 CH341A 的一行在 W10 的履歷句型裡,而且那行已經錯兩處(`2MB`、`RTL8196C`)。

**所以燒錄器不在關鍵路徑上。** 它降級成兩件事:dump 的第二來源,以及 bootloader 死掉時的最後手段 —— 而唯一會弄死 bootloader 的是我自己對 `0x000000` 下 `FLW`,那件事永遠不會做。

### 但「不寫 flash」是個假前提

這點要寫下來,因為它反過來決定了 dump 的優先序:

**W06 的 PoC 本身就在寫 flash。** W04 定位到的那一行是 `sprintf(buf, "flash set HW_WLAN0_WSC_PIN %s", localPin); system(buf)` —— `flash set` 寫的就是 `0x00C000` 的設定區。W07 的 fuzz 更不用說。

**我不是那個握著寫入器的人,不代表沒有人在寫我的 flash。**

而 `0x006000` 的 `H601`(MAC + 射頻校正)**全世界只有這一台有**。官方 `.web` 裡沒有,恢復出廠設定救不回來。`0x180000` 那個 2018 rootfs 也一樣 —— 公開下載頁上不存在這個 build。

**所以今天要拿的不是一份 dump,是唯一一份備份。** 沒有它,所有救援路徑(web 升級、TFTP、`FLW`、CH341A)都是空的,因為它們全都需要「要寫回去的那些 byte」。

### Day 4 下半:console 路開跑,以及三個工具 bug

**pin 3 = RX 從推論升級成量測。** 這是 `uart-pinout.md` §5 掛了兩天的第一項。
之前的理由是「其他三支都確定了,4-pin 的 `UART` 排針,沒有別的可能」—— 那是
消去法,是論證不是量測。今天 ESC 送進 pin 3 打斷了開機、`FLR`/`DB` 送進去被執行,
**這支腳接受輸入並且板子照做了**。

#### 工具 bug #7:ESC 把 bootloader 的輸入緩衝區塞滿了

搶到 `<RealTek>` 之後,`?` 回 `Unknown command !`。**而 8/15 那場 session 用
同一條 `?` 拿到過完整命令表。**

兩場 session 對同一台裝置給出相反結果 —— **命令表不會自己改,所以是儀器在說話。**

原因是我自己:搶 bootloader 要**連續串流** ESC(窗口只有約一秒),bootloader 吃掉
一個用來中斷開機,**其餘全部排在它的輸入緩衝區裡**,所以第一條真指令是帶著一串
ESC 前綴送達的。

**這比表面嚴重:自動 dump 的第一條指令就是陽性對照。** 一個安靜地沒有執行的對照,
比沒有對照更糟 —— 沒有對照你知道自己沒驗,有一個假的對照你以為驗過了。

#### 工具 bug #8:解析器把裝置吐的每一行都判成不合法

第一次跑 dump,死在陽性對照,訊息「no data lines at all」。

`DB` 的輸出後面有一個 ASCII 欄,我的正則沒有它。而**根因不是正則寫錯,是我從哪裡
抄格式** —— 我照 `notes/flash-layout.md` 裡引用的 transcript 寫,那份引用為了排版
把 ASCII 欄修掉了。

**最難看的部分:逐字的原始格式一直在這個 repo 裡。** `RUNBOOK.md` §8.7.8 有完整
transcript,ASCII 欄好好地在那裡,console 上線那天就寫下來了。

**沒有東西遺失,是我讀錯了文件。** `notes/` 是分析,引用會被編輯;RUNBOOK 是操作
記錄,transcript 是逐字的。**混淆這兩者,就是把摘要當成規格。**

#### 工具 bug #9:守衛套件跟被測程式共用同一個錯誤假設

而且套件當時 **10/10 全綠**。

因為那些合成 transcript 也是我照同一份引用寫的。**測試跟被測程式共用假設的時候,
它不是第二來源,它是同一個來源用兩次。**

這跟 W03 的 sink 普查、W04 的參數追蹤器是同一個形狀的錯 —— 只是這次是透過**文件**
傳染的,不是透過程式碼。

修法三層,不只補正則:正則用「至少兩個空白」當 ASCII 欄邊界(bytes 之間只有一個
空白)· 保留每行 16 byte 的長度檢查當第二層 · fixture 換成真機抓的格式,**再加一個
對抗案例:ASCII 欄的內容故意長得像更多 hex(`ab cd ef 01 23 4`)**。

改完 12/12,而且那個對抗案例是新的。

#### 工具 bug #10:監看程式每 10 分鐘安靜地失敗一次

dump 要跑 95 分鐘,所以掛了一個監看:每 10 分鐘回報進度,結束或死掉就通知我。

它從頭到尾沒發過任何進度。

原因:我用 heredoc 把腳本寫進 WSL,而**外層在送進去之前就把 `$LOG` 剝掉了**
(今天稍早撞過同一層:`X=abc; echo $X` 印出空的)。所以腳本裡每個 `"$LOG"` 到達
bash 的時候都是空字串,`tr ... < ""` 每 10 分鐘失敗一次 —— **失敗訊息寫到 stderr,
而監看只把 stdout 當事件**。

**一個安靜失敗的監看,比沒有監看更糟:「沒有通知」和「一切正常」長得一模一樣。**

修法是整支腳本不用任何 shell 變數(路徑寫死),繞過那一層。**而且這次先乾跑一次
再掛上去** —— 上一版是沒驗就上的,這就是為什麼它能安靜地爛 40 分鐘。

今天四個 bug(#7 ESC 塞緩衝區、#8 解析器沒有 ASCII 欄、#9 測試共用同一個錯誤假設、
#10 監看安靜失敗),**沒有一個是工具自己的自我檢查抓到的**,全部是「兩個應該一致的
東西不一致」抓到的。專案到現在十個工具 bug,這條規律一次都沒破。

#### 速率是物理上限,這件事要算得出來而不是「試過沒用」

`DB` 每 16 個 data byte 送 81 個字元(位址 8 + `: ` 2 + hex 48 + 空白 5 +
ASCII 16 + CRLF 2)= **5.06 倍膨脹**。38400 8N1 = 3840 B/s ÷ 5.06 =
**759 B/s 上限**,實測 **723 B/s = 理論值的 95%**。

**線吃滿了,主機端怎麼調都沒用。** `DW` 印 word 可以省約 19% 字元,但要換一套
解析器和一組新的守衛案例 —— **正確性優先於 19%**,而且 95 分鐘是機器的時間不是
我的時間;會痛的是「重跑一次」,所以該投資的是自動重讀和「拼不完整就不吐檔案」。

### 結果:4 MiB 下來了,而且它抓到廠商修到一半

105 分鐘,256 塊,**零重讀**,`sha256 a800059a…`。

驗證有四層,而且沒有一層是「工具說它成功了」:陽性對照(8/15 另一場 session 記下的
`0b f0 00 04`)· 逐塊結構驗證 · 抽 12 塊重讀(全部一致)· 21 個硬結構檢查對照
**在這份映像存在之前就寫下來的期望**(W01 三週前從廠商容器推導的燒錄位址)。

**但最硬的一層不在那四層裡:`0x180000` 的 SquashFS 解得開。** 1.8 MiB 的 LZMA 不會
從壞掉的資料裡意外解開。161 檔案、20 目錄、88 符號連結。

然後三版橫著讀,出來的東西比我預期的好:

| | 2015 | **2018(這台)** | 2020 |
|---|---|---|---|
| `/bin/skt` | **在,可執行** | **刪了** | 不在 |
| `rcS` 的 `#skt&` | 註解掉 | **註解還留著** | 移除 |
| `onlime_r` uid 0 | **在** | **還在** | 移除 |
| `passwd.org` | — | **跟 2015 完全同一個檔** | 改名 |

**廠商對 2015 那次揭露的處理,分三步、跨五年,而中間那一步在官網下載頁上不存在。**
CVE-2015-9550(帳號)和 9551(RCE)是一起揭露的 —— **兩年半之後,他們修好了其中一個。**

沒有這台機器,這條時間線只有頭和尾。「刪了 binary 卻留著 uid 0 帳號、而且那行死掉的
`#skt&` 還留在啟動腳本裡」這個中間狀態,是**只有從自己的矽晶片上讀出來才看得到的東西**。

### 差一點犯的那個錯(而且這個 repo 已經有它的更正紀錄了)

第一輪比對 `passwd.org` 的時候,2020 版回我 `No such file or directory`。

**差一點就寫「2020 版移除了密碼模板」。** 它沒有 —— 它**改名成 `passwd_orig`**,
檔案還在,`root:zhxPr1e7Npazg` 也還在。

這就是 W01 那個「兩份映像裡都沒有 `/etc/passwd`」的同一個形狀 —— 當時是把一個懸空的
符號連結讀成檔案不存在,W04 把它推翻了。**同一個 repo,同一種錯,隔了兩個月又來一次。**

擋下來的不是我比較小心,是我**去整棵樹搜 `onlime_r` 這個字串,而不是去測一個檔名**。
帳號才是被問的東西;它住在哪個檔案裡是實作細節 —— **而實作細節正是廠商會在版本之間
改掉的東西。**

同一個陷阱在這件事裡還有更利的一版:`/bin/skt` 在 2018 不見了,**停在這裡就會寫成
「2018 版修好了 2015 的後門」—— 而那是錯的,因為那次揭露有兩個後門。**
找到一個被修好,對另一個什麼都沒說。**而「找到修好了」的那個瞬間,正是人會停止尋找的
時候。**

### G2 過了

第二趟也下來了:105 分鐘、零重讀、`sha256 a800059a…` —— **跟第一趟一模一樣。**

而且我沒有拿工具自己報的雜湊當數:用 `sha256sum` 對兩個檔案獨立重算,再用 `cmp`
逐 byte 比 —— **4,194,304 個 byte,零差異**,而兩趟是經過**不同的 RAM 區域**
(`0x81000000` vs `0x81800000`)搬運的。

G2 四格全滿,而且**每一格都是強的那個版本,不是 fallback 版本**:

| | |
|---|---|
| 活 bootlog | 量出來的腳位、量出來的鮑率,而且邏輯分析儀解出逐字元相同的第二份 |
| SPI dump + hash | 兩趟完整讀取,不同 RAM,獨立重算,`cmp` 零差異 |
| dump vs 官方比對 | 而且比出了公開映像看不到的中間狀態 |
| PCB 標註照片 | 從 JSON 算圖,不是影像編輯器畫的 |

**而它是在「把燒錄器量過之後決定不用它」的情況下拿到的。**

> ⚠️ 但這一句要一起寫下來,而且我在三個地方都寫了同一句:
> **兩趟都走 bootloader 的 `FLR`,所以「`FLR` 系統性讀錯」對這兩趟都是隱形的。**
> 兩個 hash 相同證明的是**傳輸和 SPI 讀取穩定**,不是**讀得對**。
> 不經過 `FLR` 的獨立檢查只有兩個:W01 三週前預測的燒錄位址全部命中,以及
> SquashFS 解得開。**那兩個都不等於第二個儀器。**

### 明天 / 下一步

1. ~~走 console 拿完整 4 MiB~~ → **done,兩趟,G2 過了。**
2. **把 2018 的 `boa` 丟進 Ghidra,三版橫著讀。** 抽出來、算了雜湊 ≠ 讀過了。
   在它進 Ghidra 之前,這個 repo 裡每一條關於 `boa` 的結論都還是在講兩個這台機器
   沒跑過的 binary。
3. `COMPCS`(`0x00C000`)對 `COMPDS`(`0x008000`)做差分 —— 順便回答
   `TELNET_ENABLED` 預設開不開,那決定 `root`/`123456` 到底值多少。
4. CH341A 的 pin 28 補量一次。**第二個儀器那一格還是空的**,而且我要讓它空著,
   直到真的有第二個儀器。
5. 那顆跳線的實驗;`LSP5526`(從 Day 2 拖到現在)。

---

## 決策 — 2026-08-16:遮蔽政策改成逐欄判定,而且這一欄我決定全開

今天 W04-2 的規劃排到 `COMPCS` / `COMPDS` 解碼那一格,卡在一個不是技術的問題上:
解出來的東西哪些可以進 repo。

**我的決定:全開。** 理由三條,都是真的,我不打算把它們包裝得更好聽:

1. **這台是自購的 EOL 裝置,而且我不會再拿它做任何用途。** 它不在任何網路上,
   從 W02 到現在唯一連過的東西是我自己的 USB-TTL 和一條網路線。
2. **它的設定幾乎等於出廠值** —— `COMPCS` 和 `COMPDS` 前 58 個 payload byte 只差一個
   (`flash-layout.md` §6),長度只差 3。也就是說它從來沒有被真正部署過,
   裡面沒有一個值是「某個真實網路的密碼」。
3. **MAC 不是憑證。** 它是識別碼,而識別的對象是一台我自己買的、已經不用的路由器。

### 被提出來、被我否決的反對意見(留在這裡,因為它是這個決定的一半)

規劃的時候有一個反對意見,我聽完了才決定不採納,它有三點:

- **SSID 幾乎確定是 `TOTOLINK_<MAC 後六碼>`,WPS PIN 在 Realtek SDK 傳統上從 MAC 導出。**
  公開它們等於用另一條路把 W02 花一整天遮掉的那組數字送出去 ——
  而且照片還在,讀者可以拿 SSID 回去對那張塗黑的條碼。
- **真正的代價不是隱私,是自相矛盾。** repo 裡現在有六個地方承諾了相反的事,
  其中兩個在程式碼裡、一個是會失敗的測試。任何讀這個 repo 的人 grep 到
  `must_never_be_committed: [… COMPCS …]` 旁邊躺著一個 committed 的 COMPCS,
  那比任何資料本身都難解釋。
- **全開在分析價值上幾乎買不到東西。** 有價值的是格式、id→name 對應、
  哪些 id 跟預設不同、以及**預設值本身**(`COMPDS`,本來就不含個資)。
  全開只多了那幾個 per-unit 的值。

**我否決它的理由是第 1 和第 3 點:這台不會再用,MAC 不是憑證。**
第二點(自相矛盾)我接受,而且它是**行動項而不是反對意見** —— 見下面。

### 這個決定的正確形狀不是「全開」,是「逐欄判定」

一個立場(全開 / 遮蔽)只能被質疑;一張逐欄的判定表可以被檢查。
所以 `fwrecon compcs` 的輸出要帶一欄 `disclosure`,每一個欄位一句理由:

```
SSID          open   出廠格式本身就是發現:TOTOLINK_xxxxxx 洩漏 MAC 後六碼
ELAN_MAC_ADDR open   自購 EOL 裝置,不再使用;MAC 是識別碼不是憑證
USER_PASSWORD open   等於出廠預設,本來就是公開資訊(而這正是 CVE-2019-19823)
WLAN_PSK      open   同上,且此裝置從未部署
```

**逐欄寫理由,比宣告一個立場強。** 別人問「你為什麼公開 X」,我有一行答案,不是一個態度。

### 測試留著,而且變成一個開關

遮蔽的**強制機制**不刪。`flashdump.py` 那條「印到就 fail」的規則和它的測試繼續存在,
只是政策從寫死變成參數:

```
fwrecon compcs … --disclosure open       # 今天的決定
fwrecon compcs … --disclosure protect    # per-unit 識別碼以 sha256 呈現
```

兩個模式各有自己的測試,而且 `protect` 模式的測試**必須是那個會失敗的版本** ——
把已知的 MAC 字串塞進假的 COMPCS,斷言它不出現在任何輸出格式裡。

理由很簡單:**我今天改的是政策,不是能力。**
一個因為政策改變就被刪掉的執行機制,下次政策改回來的時候不會自己長回來。
而且下一台機器可能不是我的。

### 因此必須在同一個 commit 改掉的六個地方

| 位置 | 現在寫著 |
|---|---|
| `dumps/MANIFEST.json` | `must_never_be_committed: [… COMPCS at 0x00C000 …]` |
| `notes/flash-layout.md` §6 | 「值不會出現在這個 repo 或任何離開這台機器的 log 裡」 |
| `notes/dump-vs-official.md` §4 | 「`0x006000`–`0x010000` is never published」 |
| `notes/img/README.md` | 「只有對這個型號成立的東西才發布」,並點名 flash config 分區 |
| `PROGRESS.md` W02 Day 4 | 「Per-unit secret regions are reported by digest and never printed」 |
| `tools/fwrecon/src/fwrecon/flashdump.py` | `What is never printed` / `never content` |

**照片的遮蔽不動。** 那是另一件事:照片上的是**序號 QR**,QR 會被自動解碼、
而且在大幅縮圖後還活著,它跟「一個我寫在 note 裡的十六進位字串」不是同一個風險等級。
政策改的是「我主動解碼並發布的設定值」,不是「順手拍進畫面裡的東西」。

> 📌 **這一條要記在最上面:今天改的是一條我自己在 W02 寫下的規則。**
> 它不是被忘記的,是被複審之後推翻的 —— 而複審的紀錄就是上面那段「被否決的反對意見」。
> **一個沒有記下反對意見的決定,讀起來會像一個偏好;記了的,讀起來是一次判斷。**

### 回溯:兩個「理由不轉移」的判定

把新政策套回舊資料的時候,有兩項我判定**不動**,而且理由要寫清楚,
因為它們看起來都像「應該一起開」:

**1. EXIF 繼續剝除。** 今天的理由是「這台機器已 EOL、我不再使用它」。
**那句話完全不涵蓋 GPS。** GPS 標定的是**人**的位置,不是裝置的狀態 ——
而人不會 EOL。相機的裝置識別碼同理。
**遮蔽的對象從「裝置識別碼」換成「人的識別碼」時,理由不會跟著轉移。**

**2. 4 MiB 的 dump 繼續不 commit。**
`dumps/README.md` 當初寫的是「兩個獨立理由,任一個都足夠」:
(1) 這是廠商的韌體,本專案不轉散布;(2) 它含這台的秘密。

**今天理由 2 消失了,理由 1 一個字都沒變。**
所以 dump 的狀態不用改,而且我不必重新論證一遍 ——
**當初把兩個理由分開寫、而且標明「任一個都足夠」,就是為了今天這種時刻。**
這是這個 repo 的方法論在自己身上生效的第一個實例,要寫進 `dumps/README.md`。

順帶一提,最該補的那一項不是設定值,是**兩份 UART log**:
`dumps/MANIFEST.json` 自己記著 `contains_unit_identifiers: false` ——
**它們根本沒有識別碼,被擋住純粹是一條一般性規則的溢出效應。**
而它們有實質分析價值(kernel banner、記憶體配置、驅動版本、開機時序)。
進 git 之前還是要自己讀一遍全文,因為那個 `false` 是**上次**讀的結論。

完整的逐項判定表在 `plan/W04-2_補課週.md` Day 5 時段 C。

### 2026-08-16 補記:被要求覆核我自己剛寫下的四條「假前提」,其中一條我寫得太滿

規劃 W04-2 到 W10 的時候,我列了四條原計畫的錯誤前提。被要求覆核之後,
**一條需要降級,一條需要放大,而且我漏掉了一個更好的方案。**

#### 1. Lexra —— 方向對,但「子集跑在超集上」這句話是錯的

我寫「Lexra 是 MIPS 的子集,子集永遠能在超集的模擬器上跑」。
去查了 binutils 的 Lexra patch(LX4180 / RLX4181 / RLX5281)之後:

**Lexra 不是嚴格的子集。它拿掉四個(`lwl`/`lwr`/`swl`/`swr`,US 4,814,976 專利),
但也加上一整組自家指令** —— MAC 類、`lt`/`st`/`ltp`/`lwp`、RADIAX DSP ——
**放在標準 MIPS 用不到的 opcode 空間裡,而且沒有重新指派任何一個標準指令。**

所以結論仍然成立(而且證據不是文獻,是 **W01 收工時 `/bin/boa` 真的在
`qemu-mips-static` 下印出了 usage 文字**),但理由要改成:
**用到標準子集的部分跑得動;用到 Lexra 擴充的話 qemu 會 SIGILL —— 而那是大聲的失敗。**

**而覆核翻出一個比模擬嚴重得多、這個 repo 從 W03 到現在都沒點名過的風險:**

> Lexra 的擴充指令就在 **Ghidra 標準 MIPS 模組不認得的 opcode 空間**裡。
> **qemu 遇到它會大聲失敗,Ghidra 遇到它會安靜地解錯。**

反方向的證據是強的 —— 標準 MIPS 拆解還原出 59 個 handler、分派器自己的 `+2` stride、
一個讀得通的授權閘門,**一個大量解錯的拆解器產不出這種一致性**。
但「大量」不等於「全部」。所以 W04-2 Day 2 的助憶碼普查從「有沒有 `lwl`」
擴大成 **「有沒有 byte 沒被拆成指令、有沒有指令在上下文裡講不通」**,
而且加一個免費的第二來源:同一個 binary 丟 `qemu-mips-static` 跑,看會不會 SIGILL。

順帶一提,公開資料說 **rtl8196b 之後 Realtek 自己把那四個指令補回來了**,而這台是 8196**E**。
那讓「有 `lwl` = 這顆 CPU 支援」這個判讀更可能成立,但**它仍然不是量測**。

#### 2. `formSysCmd` —— 我把已驗證的範圍寫超過了

我寫「不在任何一版的分派表裡」。**已驗證的只有 V2.1.2 和 V3.4.0。**
V2.1.1 和 2018 這台自己的 build 要 W04-2 Day 1、2 才會檢查。
主張本身(不該排一整天去打它)不變,措辭降級。

#### 3. 盲注 —— 對,但我漏了一個明顯更好的 oracle

我設計了 ICMP / TCP 回連 / 時間 / flash 差異四個帶外通道。**漏了最好的那個。**

`DocumentRoot` 是 **`/var/web`**(`anatomy-n150rt.md` 早就記著),而 `rcS` 開機做
`cp -rf /web/* /var/web/` —— 也就是 **`/var` 是可寫的,而且它就是 web server 在服務的目錄**。

```
localPin=1;id > /var/web/x.txt;#     然後  GET /x.txt   →  uid=0(root)
```

**原版計畫要的「看到 uid=0(root)」其實是對的,只是它找錯地方 ——
不在回應 body 裡,在另一個 URL 上。**
寫進 tmpfs,重開機自己消失,一個 byte 都不碰 flash。
順序改成 ICMP(零副作用)→ docroot(拿輸出)→ flash 差異(最強證據)。

**這一條是我自己犯的錯,而且形狀跟這個 repo 記錄的十個工具 bug 一樣:
我設計了四個 oracle,沒有一個是去翻自己筆記裡已經寫著的東西找到的。**
`anatomy-n150rt.md` 從 W01 就寫著 `DocumentRoot /var/web`。

#### 4. `strings | grep form` —— 無需修正

還原出來的 `root_form[]` 帶位址、帶處理函式、帶輸入 SHA-256。這條成立。

#### 決定:買 CH347T

理由不是它比較新,是**它讓「先驗證儀器」這件事不用賭**:CH341A 的 5 V 是量出來的,
改壓失敗;CH347T 的 25xx 腳位設計上就是 3.3 V。
flashrom 從 **1.4(2024-07)** 起有 `ch347_spi`,而且 `spispeed` 可調 ——
夾在已焊接的晶片上必須降到 `937.5K`~`1.875M`,**預設的 15M 會給你一份格式完美的錯資料**。
⚠️ 這台目前是 apt 的 flashrom **1.3.0**,沒有這個 programmer,要自己編,**G0 的版本表要更新**。

**這是全專案投報率最高的一次量測,而且兩種結果都好:**
跟 `FLR` 的 `a800059a…` 相同 → G2 空了三個月的那一欄填上「兩條完全獨立的路徑,逐 byte 相同」;
不同 → 那是這個專案最大的一個發現。**一個不管往哪邊倒都有價值的實驗,值得優先做。**

---

## W04-2 — 2026-08-16:把結論搬到這台真的在跑的 binary 上

### 一句話

**這一週存在的理由,在第一個小時就兌現了:`formSysCmd` 在這台的分派表裡,
而兩個下載得到的映像都沒有它。** W04 把它的缺席寫成「這讀起來像廠商的修補」,
那句話今天收回。

### 三個獨立來源說同一件事

| 來源 | 說了什麼 |
|---|---|
| `BoaFormTable`(Ghidra) | `root_form[]` 第 42 格,entry `0x004838a8` → handler `0x0044ee2c` |
| `grep -aoc formSysCmd`(完全不經過 Ghidra) | 2015 = **0**,2018 = **1**,2020 = **0** |
| `BoaGate`(今天新寫的,走完全不同的路徑) | R2 在 `0x0044ef2c` 抓到 `sysCmd` → `system()` |

**缺席 → 出現 → 缺席。** 一個修補不會在兩年半之後自己長回來。W04 明確排除掉的
那個解釋(build flag)才是證據支持的那個。

而且方向是反的:CVE-2019-19824 的公告寫 "N150RT through 3.4.0" 受影響,
W03/W04 讀了兩個公開映像之後說「這個 handler 不在」。**兩個下載得到的映像,
剛好都是沒有它的那兩個。** 任何人拿公開韌體去重現這個 CVE,都會得到「不受影響」,
而那個結論對這台機器是錯的。

### 為什麼它未認證可達

這台的閘門只有在 URI 含 `.htm` 或 `.asp` 時才跑。`/boafrm/formSysCmd` 兩個都沒有。

```
0040beb8  beq v0,zero,0x0040c0a0    ; 兩個都沒中 → 直接跳過 send_r_unauthorized
0040c0a0  jalr t9 -> translate_uri  ; 正常流程從這裡繼續
```

反編譯器在這個函式上出了三個警告,所以**每一個分支都是先用 `BoaListing` 在
指令層級讀完的**,反編譯的 C 只在兩者一致的地方引用。這是 repo 的規矩,今天是
它第二次值回票價。

### 我今天犯的三個錯,都寫下來

**1. 用大小寫敏感的 `grep` 得出結論。** `grep -c Authorization` 在 2018 上回 0,
我一度寫下「2018 版拿掉了 HTTP Basic 認證」。**字串在那裡,是大寫的。**
不分大小寫是 3 / 1 / 1。真正的差別是別的:V2.1.2 額外帶著兩份寫死的
`Authorization: Basic YWRtaW46YWRtaW4=`,base64 解出來是 `admin:admin` ——
W03 和 W04 都沒記錄過這個。

**一個 `grep` 回 0 也是一個主張,而這一次它主張的是我自己的正規表示式。**

**2. 說「2018 版保留了符號表」。** 三個 build **都沒有**靜態符號表,`nm` 對三個
都說 `no symbols`。它保留的是 422 筆**動態**符號,而且同時是 `sstrip` 過的 ——
這是個更有用的事實,而我是看到 Ghidra 叫得出 `handleForm` 就推論機制,沒去查。

**3. 順序做反了,而且它剛好沒出事。** 週計畫用粗體寫著:先從 `libapmib.so`
讀解壓器,再對照公開資料。我是先從資料猜出 LZSS 參數,再去讀
`Decode` @ `0x00012e98`。**全中** —— N=4096、F=18、threshold 2、ring 起點 4078,
連 `0x20` 的填充值都對。

所以這次沒有代價。**但「這次沒有代價」是讀完 binary 之後才知道的,那正是那條
規矩存在的全部理由。** 而且 binary 給了兩樣猜不出來的東西:一個資料裡完全看不見
的 8-bit 檢查碼(現在是這個解碼器最強的正確性檢查),以及廠商自己的邊界
`if (0x3fff < len - 1U) return 0`。

**猜得準不是方法。它只是在你剛好猜對的那些天,跟方法看起來一樣。**

### `BoaGate` 的正對照,第一次跑就抓到閘門是壞的

新寫的 CI 閘門,在 V2.1.2 上回報 **0 findings** —— 那個 build 是 W03/W04
一行一行讀出 34 個問題 handler 的那一個。**兩次,兩個不同的原因:**

1. 用名字比對 sink,但 libc 是透過 `sstrip` 過的 PLT 呼叫的,Ghidra 叫它
   `FUN_xxxxxxxx`。**這是這個專案第三次踩到同一個坑**(W03 `BoaSinks`、
   W04 `BoaArgTrace`、今天)。
2. 修掉之後,字面值解析只檢查 `isConstant()`,而 MIPS 的字串位址是 lui/addiu
   湊出來的,所以**一個參數名字都沒讀到**。

**兩個都會以「這個 build 很乾淨」的形式出貨。** 前兩次這個 bug 都是出貨之後
靠橫向比對才抓到的;這一次 `control:30` 在任何數字離開腳本之前就擋下來了。

修法是把 `constAddr` 從 `BoaArgTrace` 共用出來,而不是再實作一次 ——
跟 W04 對 PLT 得到的結論同一句話,只是從另一端走回來。

### 一個沒預料到的發現:這台的版本號

`/etc/version` 寫著 `TOTOLINK-CX-N150RT-V2.1.6-B20171121.1002`。

- **這推翻 W02 的一個推論。** W02 寫著「這家廠商在發版時重建 userland,時間戳
  跟著發版日期走」。這裡版本標籤是 2017-11-21,四個 binary 全部戳 2018-01-10,
  **差七週。**
- **而且 V2.1.6 看起來是公開版本 —— 但這句話要打折,而且我第一次寫得太滿。**
  我**沒有讀到那個下載頁**。我讀到的是搜尋引擎索引裡的標題和 URL;頁面本身
  對 PowerShell HEAD、三種 UA 的 `curl`、`WebFetch` 全部回 403。
  `firmware/SOURCES.json` 從 W01 就記著 Softpedia 擋腳本抓取,所以這是**確認**,
  不是發現。archive.org 只有 2.1.2。
  搜尋摘要另外說 2.1.6 發布於 2017-05-08 —— **那跟版本字串裡的 B20171121 差半年,
  而我讀不到頁面,所以那個日期不引用。** 搜尋摘要不是來源。
  **但用瀏覽器點一下就能抓,而那一下決定了一件大事:** 如果兩份映像一致,
  這個專案最值錢的那條「廠商跨五年三步走」時間線就不再只靠我這台的 flash,
  而是別人可以重現的;如果不一致,那個 `CX` 值得弄清楚。

### `TELNET_ENABLED = 0`,而這句話要講得精確

設定區解出來是 `0`。第二來源是 `/bin/sysconf` 自己:

```c
FUN_00403400() { apmib_get(0xbbb, local_10); return local_10[0]; }   /* TELNET_ENABLED */
/* setinit: if (FUN_00403400() == 1) system("telnetd & >/dev/null 2>&1"); */
```

兩個不共用任何程式碼的來源同意。**所以 `root:123456` 在這台上不是入口,
是提權鏈的第二段。** 把它講成入口會誇大整整一步 —— 而這正是 W04 遺留問題 #4
當初想問的東西。

順帶一個沒人記錄過的:`SSH_PASSWORD = xa.zioncom`,出廠預設,`COMPDS` 和
`COMPCS` 逐 byte 相同 —— **所以它是關於這個型號的事實,不是關於這台的。**
但這個 rootfs 裡**根本沒有 SSH daemon**,`dropbear` 只出現在一個 killall 清單裡。
一個沒有東西可以啟動的旗標。這個限定條件是主張的一部分,不是註腳。

### 揭露政策落地,以及一句當初寫對的話今天兌現

`dumps/README.md` 當初寫的是「**兩個獨立理由,任一個都足夠**」。今天
per-unit 機密那條失效了,「不轉散布廠商韌體」那條沒有 —— 所以 raw dump 的狀態
**一個字都不用改**,而且我不用重新論證一遍。

**當初把兩個理由分開寫、並標明「任一個都足夠」,就是為了今天這種時刻。**
這是這個 repo 的方法論在自己身上生效的實例,值得單獨記一筆。

兩份 UART log 進 repo 了 —— 但是**先自己重讀過全文才 add 的**,因為 MANIFEST
那個 `contains_unit_identifiers: false` 是**上一次**的結論。順帶學到:
`uart-boot.log` 有三個不可列印字元,所以純 `grep` 會說 `binary file matches`
**而不告訴你比中什麼** —— 那個輸出讀起來跟「找到東西了」一模一樣,實際上相反。
要加 `-a`。

### G3.5 過不了,而且是我做不到的那一格

五條裡四條完成。第五條是 `FLW` 回復路徑演練,**需要人、需要萬用電表、需要那台
機器**。`RUNBOOK.md § 8.9` 已經把整套流程寫成可以直接貼的逐字步驟,含每一步的
預期輸出和三條保護措施。

**W05 不准在那一格完成之前開始**,理由跟 G3 一樣,而且更實際:W06 的 PoC 必然
寫 flash,而這台的回復路徑從來沒有被執行過。

---

## 2026-08-16 補記 — V2.1.6 抓回來了,而且我替它寫的那個論證是錯的

上面那段〈一個沒預料到的發現:這台的版本號〉結尾寫著「用瀏覽器點一下就能抓,
而那一下決定了一件大事:如果兩份映像一致…如果不一致,那個 `CX` 值得弄清楚」。
**那個「如果」當天稍晚就有答案了,而且兩邊都不是。**

那段文字一個字都沒改。**它是當時的認知狀態,改掉它這份 LOG 就沒有價值了。**

### 答案:同一個版本號,兩個 build

| | |
|---|---|
| 抓到的 | `TOTOLINK-N150RT-V2.1.6-**B20160516**.1233.web` |
| 這台跑的 | `TOTOLINK-CX-N150RT-V2.1.6-**B20171121**.1002` |

同一個產品版本號,兩個相差十八個月的 build,而且公開版沒有 `CX`。
所以 W02 那句「這個 build 在任何下載頁上都不存在」**成立**,只是要講精確:
**版本號是公開的,這個 build 不是。**

### 我寫的連續性論證是錯的,而且是可以測出來的錯

當時為了回答「你怎麼知道第三方鏡像上那個檔沒被動過」,我寫了:kernel section
長度 985,090 → 986,114 → 987,138,**每步剛好差 1,024**,「一個被改過的檔不太可能
剛好落在那條線上」。

把四個 build 全部算一遍就散了:

```
2.1.2 (2015-08)    985090 =  962*1024 + 2
2.1.6-B20160516    986114 =  963*1024 + 2     step +1024
unit-2018          987138 =  964*1024 + 2     step +1024
3.4.0 (2020-10)   1234946 = 1206*1024 + 2     step +247808
```

**四個 build、橫跨五年,全部 ≡ 2 (mod 1024)。** 那不是巧合,是這個 section
對齊到 1 KiB 網格。「每步差 1,024」= 三個連號的網格點;而 2015 和 2018 之間
**只有一個網格點**,所以任何尺寸落在那個區間的正常 kernel 都會**由構造保證**
落在 986,114 上 —— 被改過的也一樣。

我把**量化的格線當成巧合**。`w6cg` 那半沒對齊(餘數 642 / 868 / 532),所以
「夾在前後版本之間」是真的,但那只是一個約 32 KiB 窗口的排序測試,是弱佐證,
不是「不太可能」。

> **這是這個專案第四次「兩個東西應該一致所以我去比」抓到問題,但前三次是工具
> 在騙我,這次是我自己的論證。** 分別在於:前三次我比的是兩個獨立來源,
> 這次我只有一組數字,是**多拿一個 build 進來算**才看見的。
> 三個點看起來是趨勢,四個點才看得出那是格線。

### 換上來的證據比原來那個硬

| 來源 | 值 |
|---|---|
| ZIP local file header 的 **DOS 時間戳** | `2016-05-16 12:34:30` —— 跟檔名的 `B20160516` 是**兩個不同欄位**,檔名是文字,這個是打包程式寫的 |
| kernel **壓縮 payload 內部** | `Linux version 2.6.30.9 (acer1@localhost.localdomain) ... #1338 Thu May 12 21:05` —— 改名改不到;2016-05-12 是星期四,比打包早四天 |
| 同一條 cmdline | `console=ttyS0,38400 root=/dev/mtdblock1` —— 對得上 W02 在**這台硬體上**量到的 26µs bit time |

**天花板沒有變:TOTOLINK 不簽章,所以以上全部是佐證,不是來源證明。**
這句話 `SOURCES.json` 從一開始就寫對了,是我後來自己越講越滿。

### 而那份 40% 殘檔,撐得比我寫的多

我寫的是「section lengths only;沒有 rootfs、沒有 `/etc/version`、沒有 `boa`」。
後半對,前半錯:

```
w6cg  declared   296,804  present   296,804  COMPLETE  [bzip2]
cr6c  declared   986,114  present   986,114  COMPLETE  → LZMA 解到 3,374,608 bytes, eof=True
```

**三段裡有兩段是完整的。** 截斷的只有第三段 `r6cr`(rootfs),也就是
`/etc/version` 和 `boa` 住的地方。「只有 section 長度」這個講法把手上的東西
整整低估了一段 —— 而 `w6cg` 完整代表 **2016 年的 web UI 現在就能打開**,
那件事的後果另外記。

### 為什麼這一整段是「補記」而不是當天就寫進去

因為當天的 **`document sync` commit 跑太早了**。那個 commit 標題就叫
document sync,可是它後面又落了兩個 commit 的真工作,兩個都沒有回頭同步
`LOG.md` 和 `RUNBOOK.md`。結果是:`PROGRESS.md` 已經把開放問題 #1 劃掉寫
`answered: no`,而 `LOG.md` 的結尾還停在「如果…如果…」——
**一個從頭讀到尾的人,會在 LOG 最後一段以為這題還開著。**

`CLAUDE.md` 那條「RUNBOOK、PROGRESS、README board 跟工作同一個 commit」
存在的理由就是防這個,而它**在規則本身被重寫進 `CLAUDE.md` 的同一週失效**。
病因不是忘記,是把 document sync 當成一週過一次的關卡,而不是隨時要成立的狀態。

---

## 2026-08-16 補記(二)— 打開廠商真的出貨的網頁,而它跟 binary 講的是反的

上面那句「`w6cg` 完整代表 2016 年的 web UI 現在就能打開」的後果。

**這個專案從頭到尾只 grep 過 binary。** `formSysCmd` 的 `0 / 1 / 0` 是三個
`boa` 執行檔的字串計數,`root_form[]` 是從 binary 復原的分派表 ——
**沒有人打開過那個會 POST 到它的網頁。** W01 把 `w6cg` 的格式列為「解開了但
沒 parse」,列在 `attack-surface.md` §5,擺了兩週。

### 先把格式做完,而且做一個會失敗的檢查

64 bytes header + 內容,長度欄在 `+0x3c`,**big-endian**(header 裡其他欄位
都是 little-endian,只有這個不是 —— 這種細節就是猜錯會爆給你看的那種)。

**這個格式沒有校驗碼、沒有檔案數、沒有結束標記**,所以「我 parse 對了」沒有
現成的東西可以對。但結構本身給得出來:每一步都是 `64 + length`,所以走完整條
鏈**要嘛剛好停在最後一個 byte,要嘛歪掉**。三份 bundle 全部 `exact`,
1,720,168 / 1,704,011 / 1,417,000 bytes 一個不剩。測試裡我把長度欄搬到 `0x38`
(一個很合理的猜法),斷言它必須變成 `derailed`。

**而且 143 這個數字對上了。** `auth-flow-2018.md` 兩週前用臨時方法數出 2018 那份
是 143 筆,我這支 parser 沒有參考那個數字,跑出來也是 143。兩個獨立的走法在
一個沒有文件的格式上同意,比任何一邊單獨講都值錢。

### 結果

| | 2015 V2.1.2 | 2016 B20160516 | 這台 2018 |
|---|---|---|---|
| `syscmd.htm` | **在**,3,835 bytes | **在,而且逐 byte 相同** | **不在** |
| 它的 form | `action=/boafrm/formSysCmd` | 同一串 bytes | — |
| `boa` 裡的 `formSysCmd` | **0** | *不知道(rootfs 被截斷)* | **1** |
| 在 `root_form[]` 裡 | **不在**(59 筆) | *不知道* | **在**,`0x004838a8` |

**2015/2016 出貨頁面、沒出貨路由 —— 那個按鈕照程式碼讀是打到 404。
2018 出貨路由、沒出貨頁面。** 兩半是反的。

這正好是 CVE-2024-51228 揭露者那句被當成廢話的括號:*"even if the GUI
(`syscmd.htm`) is not available"* —— 現在它下面有一組量出來的 before/after。

**而且這是同一個廠商的同一個手勢:修一半。** 2015 那次把路由拿掉、把頁面留著,
跟「`#skt&` 註解掉但 `/bin/skt` 照樣出貨」、「`onlime_r` 留在 `passwd.org`」
是同一個形狀。三個洞出自同一次揭露,三次都修一半。

順帶:2018 那份已經沒有那個頁面了,但 `language_vn/sc/sp.js` 裡**還留著它的
翻譯字串和一行 `/**** syscmd.htm ****/` 註解**。又一個化石。

### 我今天在這一段犯的兩個錯

**1. 我差點拿一個 substring 去推翻一句正確的結論。** 第一次是對「整塊解壓後的
資料」grep `syscmd.htm`,三份都找得到,包括 2018 —— 看起來直接打臉
`auth-flow-2018.md` 的「143 筆裡沒有 syscmd.htm」。我當時已經準備寫成衝突了。

**它不是衝突。** 2018 那些命中在 `language_*.js` 的註解裡。**檔名出現在某個
檔案裡,不等於那個檔案存在** —— 這個差別對「整塊 grep」是隱形的,對「逐筆
搜內容」是顯然的。

真正該記的不是「grep 要小心」。是**我發現矛盾時的直覺是相信比較新的那個測量**
—— 而比較新的那個是還沒 parse、沒檢查、沒重現過的 substring 計數;比較舊的那個
有寫出方法(「143 筆,零 trailing bytes」)。**方法寫出來的那一邊才是可以被
檢驗的那一邊。**

**2. 我猜了一個 repo 裡已經寫著的東西。** 第一次走 header 的時候我把欄位順序
猜成 `tag / burnAddr / length / startAddr`,結果 296,804 bytes 的 section 讀出
65,536。正確順序是 `tag / startAddr / burnAddr / length`,而它**早就寫在
`rtlimage.py` 的 docstring 裡**。讀十秒自己的 parser,可以省掉十五分鐘的 hex。

### 這一段沒證明什麼

- **2016 那個 build 的 `boa` 我還是沒讀到。** rootfs 被截斷,所以「B20160516
  的分派表裡有沒有 `formSysCmd`」還是要重抓。上面那張表那兩格寫「不知道」,
  不是從 2015 推過去。
- **2018 的移除不是外科手術。** 2015→2018 掉了 27 筆、加了 26 筆、117 個同名檔
  有 60 個內容變了。`syscmd.htm` 是那 27 筆之一,**它是被整包改版帶走的,
  不是被單獨挑出來刪的** —— 把它讀成「針對性移除」就是在對一次改版讀心。
- **還是一個 request 都沒送出去。** 「那個表單打到 404」是對一個復原出來的
  分派陣列的陳述,不是對觀察到的回應的陳述,在 G4 之前都是。

---

## W05 Day 0 — 2026-08-17:那份紅隊手冊不該放在 plan/,但它也不該只是被搬走

紅隊測試手冊 v3.0(4,877 行)攤在 `plan/` 裡,問題是「它跟 PROGRESS.md 很像,
也有完成表和要打勾的東西」。這個不舒服的感覺是對的,而且它指向的東西比位置更嚴重。

### 先講今天最該記的一件事:那個 commit 差一步就 push 出去了

`6068fa3` 把兩份原始手冊(5,697 行)commit 進 `study/`。它**還沒 push**,
`git rev-list origin/w04-2-catchup...HEAD` = 1。工作區裡那兩個檔已經被刪掉了,
但**刪除不會把它從 history 拿掉** —— 只要 push,就永久公開。

為什麼這件事嚴重,不是因為內容見不得人:

> `README.md` 自己寫著 **not producing weaponised exploits**,以及
> **anything genuinely new goes to TWCERT/CC before any public discussion**。
> 而那份手冊裡有反鑑識、持久化、橫向移動、降版把 2015 後門裝回去,
> 以及四個沒有 CVE 的原創向量的完整打法。

一個敵意讀者三十秒就能把這兩個東西並排。他的結論不會是「作者很壞」,
會是**「這個 repo 的自我聲明是裝飾」**,然後其餘 90% 的證據紀律跟著一起打折。

處置:`git checkout` 撈回檔案 → 移到 gitignore 的 `plan/sources/` →
`git reset --hard HEAD~1`。history 乾淨,檔案本機還在。

### 真正的診斷:那不是一份文件,是三份

| | 內容 | 這個 repo 裡它的同類 |
|---|---|---|
| 狀態表 | §16 主追蹤表 130 列、⬜/✅ 欄 | `PROGRESS.md` |
| 證據型分析 | 七個錯、十一處裁決、附錄 A/B/C | `notes/*.md` |
| 攻擊執行手冊 | payload、持久化、反鑑識 | **沒有同類,因為刻意沒有** |

會撞是因為**同一份狀態被兩個檔案擁有** —— 這正是上禮拜自己寫的那篇 post-mortem
的失效模式。所以拆的方式不是「搬到哪個資料夾」,是**按擁有權拆**。

### 我原本打算插一週 W05-2,後來沒有

W04-2 插得理直氣壯,是因為 W02 發現這台跑第三個 build,**既有 gate 的完整性壞了**。
「我合併了兩份手冊」不是同一種事件。為一份文件插一週,交付物就變成文件本身。

改成:不新增週次,把「開打前」變成一個 gate(G3.75),其餘照 W05/W06/W07 既有的週跑。
而且 G3.5 #5 **沒有**搬進 G3.75,是被引用 —— 搬進去等於用改名把一格沒過的變成一個過了的 gate。

### 寫登記簿的時候,登記簿先抓到我兩個沒有證據的說法

這是今天最值得記的部分,因為**兩個都不是「找問題」找到的,是「試著寫下什麼會推翻它」
逼出來的**:

1. **`/bin/*.sh` 把設定值拼進 shell 的普查,在任何 committed 的檔案裡都不存在。**
   `grep -rln "smbbak" notes/ reports/ PROGRESS.md` 只命中一個 `sysconf` 的 xref
   JSON。`rcS` 是審過的,但那份普查只活在私有手冊裡。所以 P0-8 記成 **🔶 部分**,
   不是 ✅。
2. **「這台沒有 `nc`、沒有 `tftp`」我引用了好幾次,而它沒有被證明過。**
   `n150rt-unit-2018.json` 那 55 個 binary 數的是 ELF,**busybox applet 是 symlink,
   根本不會出現在那份清單裡**。這句話現在降級成「預測」,反證條件寫清楚了:
   如果裝置上真的有,那代表所有「這台沒有 X」的結論都要重新看。

### 一個我在文件裡寫錯、然後被自己的規矩抓到的

`notes/cve-status.md` 的草稿寫了「`prior-art.md` 照抄了 CVE 的錯誤端點拼字而沒有
註記,本檔修正之」。**不對。** W04 早就抓到了,而且更正就寫在那張表下面那一段。

我讀了表,沒讀表下面的段落。**這跟「讀了報告但沒讀它量在哪顆 binary 上」是同一個
動作。** 這一節現在留在 note 的「第一版錯在哪」裡,而不是被安靜地刪掉。

### 這一天沒證明什麼

- **G3.75 五格只過兩格。** 過的那兩格都是文件工作。要機器的三格(`FLW` 演練、
  隔離、IoC 預檢)一格都沒做,**所以還是不准送第一個封包。**
- **登記簿裡 128 項,127 項還是 ⬜。** 回填的 5 筆全部是 W01/W02/W04-2 已經有的
  證據,今天沒有產生任何一個關於這台機器的新事實。
- **21 項排進去的測試還沒有反證條件。** 大多是 Phase 6–8(W07)。工具會擋住它們
  被記錄結果,但「還沒寫」不等於「不用寫」。
- **`rtcase` 的凍結不是防篡改。** 鑰匙在我手上。它只保證「事後改預測」這個動作
  會出現在 diff 裡,不保證它不會發生。

---

## W05 — 2026-08-17:那個死結是一個檔案,而我在自己的環境裡量錯了一次

### 順序反過來跑,而且反過來是對的

計畫的 Day 1 是掃描、Day 2 才是模擬。實際上先做了模擬,理由不是偏好:
**G3.5 #5(`FLW` 回復路徑演練)擋住任何送到裝置的東西**,而模擬什麼都不需要。

反過來跑之後,發現計畫給真機用的 payload 裡有三處是錯的:

| 計畫寫的 | 實際 |
|---|---|
| oracle 0 用 `…;id > /var/web/x.txt;#` | **這台的 busybox 沒有編 `id`** |
| `/var` 可寫,因為 `rcS` 做 `cp -rf /web/* /var/web/` | 這個 build 做的是 `cd /web ; flash extr /web` |
| 「`apmib` 可能走 `ioctl`,假 mtdblock0 會失敗」(§五風險表第一列) | `lseek`+`read`。列為本週最大風險,沒有發生 |

**三處都是在一個弄壞了也沒代價的地方發現的。** 如果照原訂順序,第一處會變成
真機上一個空的 200,跟「參數被過濾」長得一模一樣。

### 我在自己的環境裡量錯了一次,而抓到它的不是任何 self-check

復原 `/dev/mtdblock0` 之後跑了一輪「乾淨」的量測,只改 `HW_WLAN0_REG_DOMAIN`,
**結果 diff 裡出現了七個 WPS PIN 欄位的 byte** —— 上一個測試留下的。

原因在 `strace` 裡從第一次跑就看得到:

```
429 ipc(2,-734995502,1,1974)     = -1 errno=17 (File exists)     <- semget
429 ipc(23,1009834962,1166,1974) = 1                             <- shmat
```

`flash` / `boa` / `sysconf` 把 MIB 表快取在 **System V 共享記憶體**裡,那段記憶體
屬於**主機**核心,活得比 guest 行程久。**我看過那幾行,沒讀懂。**

如果沒抓到,結論會是「`flash set` 會重寫整個硬體區塊」—— **跟真正的答案相反**
(真正的答案是它只改 payload 那幾個 byte 加一個校驗和),而且輸出裡沒有任何一個
地方看起來不對。

修法不是「下次記得清」,是把 `reset` 做成 `tools/qemu-env.sh` 的子指令、讓它同時
清兩邊,然後加一個守衛案例斷言前一個測試的值不會活過來。

### 兩次假警報,都是我的,不是工具的

1. **用 `ls` 數 docroot 得到 90,而 `fwrecon` 說 143。** 差點寫成「兩個儀器對同一份
   資料不一致」。`ls` 只列頂層,那個 bundle 有四個目錄,`find -type f` 就是 143。
2. **接著寫的比對腳本也錯了,方向一樣。** 它拿 `fwrecon` 存的完整路徑
   (`icons/ICON/icon_QoS.png`)去比 `find -printf '%f'` 的檔名(`icon_QoS.png`),
   於是報「兩邊各有 55 個對方沒有的項目」。

**連續兩次,兩次都是我的比對方式錯,不是被比的東西錯。** 最後是用 143 個
sha256 逐一對才收掉的 —— 而那個結果是完美的:143/143 相同。

**寫下來的理由:一個假警報如果沒被記,下一次它會以「這個工具有前科」的形式回來。**

### 三個守衛套件的 bug,而且第一個差點以三個通過的形式出貨

`tools/test-qemu-env.sh` 第一次跑,三個拒絕案例都回報「refused for the WRONG
reason」—— **三個都是同一行**:`$HOME` 在 `sudo` 下是 `/root`,而這支工具整個
都要 `sudo`。**如果那套件只檢查 exit code,它會記三個 ok。**

修掉之後,陽性對照開始**隨機失敗**:`IP_ADDR=10.1.1.1` 印出來跟期望值一模一樣
卻判不符,而 `USER_NAME` 第一次過、第二次沒過。原因是 `set -o pipefail` 加
`grep -q`:grep 一命中就結束,`printf` 拿到 SIGPIPE,`pipefail` 把整條管線判成
141。**所以命中位置在資料流中段的會失敗、在尾端的會過。**

**一個會隨機失敗的對照組比沒有對照組更糟**,因為第一件事就是重跑到它過。

`tools/test-bench-probe.sh` 也抓到一個:事後對照組記成
`{"probe": "control-after", **control(...)}`,而 `control()` 自己會設 `probe`,
**展開在後面把它蓋掉了** —— 逐字紀錄裡的事前和事後對照組長得一模一樣。

### 作業單自己踩了 RUNBOOK 記過的坑

`FLW` 演練的 Step 5(判斷會不會抹整個磁區)我讓讀回用了 `80540000`,
**而那是 Step 4 已經用過、裡面就是同一個樣式的位址**。`RUNBOOK.md` §8.7.8
早就用名字警告過:「內容沒變就是 `FLR` 沒生效」。

所以那一格的證據不足以支撐它看起來支撐的結論,而分辨它只要四行指令
(換一個全新的 RAM 位址,而且先 `DB` 一次當對照組)。寫進
`RUNBOOK.md` §8.9.3 和 `PROGRESS.md` 開放 #17。

### 一件我拒絕做的事

作者在檯子上回報「Phase 1 和 Phase 2 的輸出跟預期一模一樣」,並要我幫忙填紀錄卡。

**沒有填。** 理由是 Step 5 和 Step 6 我各寫了**兩種都正確的結果**,所以
「跟預期一樣」對那兩格字面上沒有定義 —— 而那兩格恰好是當天最重要的產出。
照著自己寫的預期去填一張帶時間戳、看起來像量測的表,**正是這份登記簿要擋的
那件事,只是換成由我來犯**。

不用重打的原因是逐字紀錄本來就在磁碟上(作業單讓 picocom 開了 `--logfile`),
所以是把 log 讀進來、從裡面填。**而讀進來之後就發現 Step 5 和 Step 6 在
NOR flash 的物理上互相矛盾** —— 那是「跟預期一樣」這句話讀不出來的東西。

### 這一天沒證明什麼

- **這台裝置一個網路封包都沒收到。** 31 個登記項目 25 個還是 ⬜,原因很平凡:
  這台筆電沒有內建網路孔。
- **`boa` 連在模擬環境裡都沒服務過請求。** Phase 2 的全部仍然是讀出來的。
- **模擬環境的 `flash set` 寫的是一個普通檔案。** 那三個 byte 是對真機的**預測**。
- **`flash test-csconf` 判什麼叫「無效」我沒讀**,所以「設定壞掉會開 telnet」
  是線索不是結論。
